# Checkpoint: claude (v2.3 platform features) — 2026-06-11

## What was done
- **Release v2.2.0**: CHANGELOG chốt (bổ sung PDF/DOCX + tool-selection thiếu), bump version, annotated tag, push origin. Fix-forward `5c5a739` BOM trong package.json (lỗi của chính release bump — PS5.1 Set-Content utf8).
- **Research workflow 6 hướng + panel phản biện 3 vai** (9 agents): verdict subscription (KHÔNG build được hợp lệ — ToS 02/2026, 4 nguồn verified), merge 2 proposal trùng, đảo thứ tự build workflow, bác gate chết. Digest: `.claude/tmp/research-digest.txt`. Plan: `docs/superpowers/plans/2026-06-11-v23-platform-features.md`.
- **MERGE `bebdc5e`** (13 commits, branch feat/v2.3-platform, worktree riêng, subagent-driven 2-vòng review mỗi task + final review):
  - Epic A: /monitoring + /graph reachable mobile (SettingsMenu rows), active-state prefix, bottom-nav clearance (eval/RF controls/editor sheet), editor 380px hết tràn (đo thật 597px), xoá /ui-preview.
  - Epic B: `format` JSON-schema trên agent node (parse + 1 retry + strip fence, fail-loud), 2 seed template judge-verify + scheduled-triage (eq trên enum, Demo-only, test khoá no-phantom-trigger-vars), 3 idiom AI-builder. PIN: workflow = local model ONLY.
  - Epic C: Claude provider MVS — adapter Messages API (`src/lib/llm/claude.ts`, whitelist sonnet-4-6/opus-4-8, system folding, max_tokens only, typed errors 529-correct), route branch không-tools, summarize PIN local, UI optgroup + cost note + est-$ từ in/out THẬT, hardening (user-first window guard chống 400 lặp, fail-loud pre-delta, tool-less system prompt, abort xuyên SDK).
  - Epic D: README MCP-mount (P6 đã ship) + Claude notes; backlog google-mcp-poc (điều kiện + kill-switch) + crypto-hkdf + SSRF nâng severity.
- **Post-merge fix `cf1d2ff`**: npm install SDK trên main (npm đã PHÁ junction worktree → dep không vào main; tsc bắt được) + dọn stale .next types. Push origin.

## Files changed
45 file qua merge (xem `git show bebdc5e --stat`) + serena (decision claude-provider-and-subscription, 2 backlog mới, INDEX, plan, checkpoint này).

## Current state
- Main = `cf1d2ff`, pushed. **1595/1595 test + tsc 0** verify trên main sau merge + install.
- Claude provider: cần `ANTHROPIC_API_KEY` trong env mới hiện picker (chưa set — operator action).
- Worktree v23-features đã remove (--force, node_modules thật bên trong), branch deleted.

## Next steps
- Operator: set ANTHROPIC_API_KEY nếu muốn bật Claude; restart dev để regenerate .next types.
- C3 (FULL: tools/vision trên Claude): điều kiện trong decisions/claude-provider-and-subscription.md (eval k≥6 + 2 contract test + mime channel).
- Visual verify các fix responsive trên :3100 khi user bật dev server (code-level + test đã xanh; editor/RF controls nên xem mắt thường ở 380px).
- Tag v2.3.0: CHƯA đánh (chờ user quyết thời điểm release).

## Blockers / Risks
- 3 fix-agent chết giữa chừng trong phiên (pattern lặp) — đều phát hiện qua git status + hoàn tất tay.
- npm-phá-junction + BOM: 2 gotcha mới đã ghi memory (user-level) để phiên sau né.
