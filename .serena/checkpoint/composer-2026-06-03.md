# Checkpoint: composer — 2026-06-03

## What was done
- Wave 3 Package W3-D — built the 3 presentational chat UI components conforming
  EXACTLY to the LOCKED prop contracts in `docs/superpowers/plans/2026-06-03-v2-wave3-chat.md`.
- TDD throughout (vitest + RTL + jsdom, renders wrapped in `<I18nProvider lang="vi">`).
- Reused EXISTING `chat.*` i18n keys — NO dict keys added (157-count test untouched).

## Files changed
- Created: `v2/src/components/chat/Composer.tsx` + `.test.tsx` (16 tests)
- Created: `v2/src/components/chat/SettingsPanel.tsx` + `.test.tsx` (6 tests)
- Created: `v2/src/components/chat/ConversationSidebar.tsx` + `.test.tsx` (13 tests)
- Plan: `docs/superpowers/plans/2026-06-03-v2-wave3-pkgD-composer.md`

## Current state
- `npx vitest run src/components/chat/{Composer,SettingsPanel,ConversationSidebar}`
  → 3 files, 35 tests, all pass. tsc clean for these three. NOT committed (per brief).
- Composer: controlled textarea; Enter=send / Shift+Enter=newline / Esc=onStop;
  slash menu (/moi /xoa /dung /xuat /caidat, filtered on "/"); char+token counter;
  📎 file (hidden input) + 🔗 URL (prompt→onAddUrl) buttons; drag-drop overlay→onAddFiles;
  attachment chips with × → onRemoveAttachment; send disabled streaming/empty; Stop shown
  while streaming; visual scroll-to-bottom button.
- SettingsPanel: model <select> (always lists current model), temp + topP range sliders,
  system textarea — every change emits full next ChatSettings via onChange. Controlled.
- ConversationSidebar: search (query/onQuery, client-side title filter), + New (onNew),
  rows (onOpen, aria-current highlight), inline rename (double-click title OR pencil →
  Enter commits / Esc cancels → onRename), delete × (confirm → onDelete), empty/no-match
  states, untitled fallback.

## Next steps (TL integration)
- TL composes these into ChatClient.tsx. Note the locked Composer props expose only
  onSend/onStop — so slash commands /moi /xoa /xuat /caidat only CLEAR the input here
  (ChatClient owns those via its own controls); only /dung is wired (→onStop). The
  scroll-to-bottom button is a VISUAL element with no callback in the locked props —
  ChatClient must wire its onClick + visibility if it wants it functional.

## Blockers / Risks
- None. Disjoint from messages/proxies/chatapi packages.
