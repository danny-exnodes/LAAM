# Checkpoint: w3-vision — 2026-06-11

## What was done
- W3 Vision: nối kênh ảnh raw (base64) vào model qwen3-vl qua `body.images` của POST /api/chat,
  SONG SONG flow OCR-text-prefix cũ (giữ nguyên).
- Client: ảnh image/* giữ b64 (bỏ prefix `data:`); cap 2 ảnh/lượt × 2MB sau encode
  (`imageCap.ts`); vượt cap → notice i18n (chat.imgCapCount/imgCapSize ×3 ngữ) + chỉ OCR-text.
- Server: `imagesError` validate (≤2 ảnh, ≤2.8MB base64/ảnh) — QUYẾT ĐỊNH: vượt cap → **400
  REJECT, không strip** (client đã degrade thân thiện; strip im lặng giấu bug — Rule 12).
- `buildOllamaPayload` gắn images vào message user CUỐI ({role:'user',content,images} đúng
  format Ollama); ảnh KHÔNG persist (lượt sau chỉ còn OCR-text). Confirm/resume path không đụng.

## Files changed
- `src/lib/agent/orchestrator.ts` (ChatMessage +images? — additive)
- `src/app/api/chat/route.ts` (+route.test.ts: 4 test mới)
- `src/components/chat/{imageCap.ts,imageCap.test.ts}` (MỚI)
- `src/components/chat/{ChatClient.tsx,Composer.tsx,types.ts}` (+2 test files append)
- `src/i18n/dictionaries/chat.ts` (+2 key ×3 ngữ)

## Current state
- Targeted tests xanh: route 12 + imageCap 3 + ChatClient 2 + Composer 19 (41/41);
  suite lân cận src/lib/agent + src/components/chat + chat dict: 231/231. `tsc --noEmit` exit 0.
- Wire-format additive: không gửi images → body/payload y hệt trước (regression test có).

## Next steps
- Runtime-verify trên host: gửi ảnh thật qua /chat, xem qwen3-vl mô tả ảnh (cần Ollama).
- Cân nhắc hiển thị thumbnail ảnh trong chip đính kèm (UX, ngoài scope W3).

## Blockers / Risks
- Cap 2×2MB là heuristic VRAM 16GB + num_ctx 16384 — chưa đo thực tế đa ảnh lớn.
- Ảnh không persist: regenerate/reload chỉ còn OCR-text (chấp nhận, đã comment).
