# Checkpoint: w7-search — 2026-06-11

## What was done
- W7: port tính năng Search từ v1 (gap parity #1 trong backlog/v1-unported).
- `src/lib/search.ts`: `searchAll(db, q, userId)` → {sessions, conversations, workflows}, cap 20/nhóm, ILIKE; sessions org-shared (latestActivity + project name + gitBranch, join projects như /api/events); conversations/workflows scope theo userId; escape `% _ \`; q.trim() < 2 ký tự → rỗng không chạm DB. pg_trgm GIN = nâng cấp sau (ghi trong comment).
- `GET /api/search?q=` (session-protected, KHÔNG thêm vào isPublic — allowlist nên mặc định đã chặn).
- Page `/search` + `src/components/search/SearchClient.tsx` (debounce 300ms, loading/error/empty/hint, link /agents/[id] · /chat thường (ChatClient không đọc query param hội thoại — đã ghi chú) · /workflows/[id]).
- Nav: thêm Search (lucide) vào app-header.tsx + bottom-nav.tsx. `nav.search` ĐÃ có sẵn trong common.ts — không thêm key.
- Dict mới `src/i18n/dictionaries/search.ts` (12 key ×3 lang) + parity test theo mẫu auth.test.ts.

## Files changed
- MỚI: src/lib/search.ts(+test) · src/app/api/search/route.ts(+test) · src/app/search/page.tsx · src/components/search/SearchClient.tsx(+test) · src/i18n/dictionaries/search.ts(+test)
- SỬA: src/components/app-header.tsx · src/components/bottom-nav.tsx · .serena/memories/backlog/v1-unported.md (đánh dấu Search done)

## Current state
- 19/19 test W7-scope xanh (lib 6 + route 3 + dict search 3 + common 2 + component 5). tsc: 0 lỗi trong file W7.

## Next steps
- Khi ChatClient có deep-link hội thoại → đổi link nhóm conversations trong SearchClient.
- pg_trgm GIN index nếu search chậm ở quy mô lớn.

## Blockers / Risks
- Worktree CHUNG đang có việc dở của agent khác: `src/i18n/dictionaries/agents.ts` +1 key nhưng agents.test.ts còn expect 71 → test đó FAIL (không thuộc W7); tsc lỗi ở src/app/api/config/route.test.ts + ChatClient/Composer (đều của agent khác).
