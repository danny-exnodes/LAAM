# Checkpoint: cto — 2026-06-07

## What was done
- Nhận brief consultant (Machines decomposition). Verify lại 7 claim load-bearing từ code (schema.ts:99, ingest bỏ RBAC, /machines redirect, machineId/source nullable, sha256+thiếu unique index, ConnectorTool.kind, memos client/host) — đều đúng.
- Ra **CTO verdict** cho 4 quyết định: Q1 token=H3 · Q2 ingest attribution-recorded/visibility-per-source (KHÔNG per-user) · Q3 MCP-server read-only laam_* trước · Q4 giữ tên "Machines".
- Append verdict vào artifact, nâng memo PROPOSED→**LOCKED**, thêm entry INDEX (consultant báo đã làm nhưng commit eb20248 thực tế chưa đụng INDEX).

## Files changed
- cherry-pick eb20248 (artifact + memo từ branch consultant)
- `.serena/memories/comms/active/consultant-to-cto-machines-decomposition.md` (+§CTO VERDICT)
- `.serena/memories/decisions/machines-decomposition.md` (status LOCKED + 4 verdict)
- `.serena/memories/INDEX.md` (+entry machines-decomposition)
- `.serena/checkpoint/cto-2026-06-07.md` (file này)

## Current state
- 4 quyết định đã chốt, ghi đủ vào memo + artifact. Thread vẫn ở comms/active (chờ consultant đóng sang resolved sau khi viết plan).
- Chưa có code thay đổi — đây là quyết định kiến trúc, P0 chưa implement.

## Next steps (consultant)
- `superpowers:writing-plans` cho **P0 Access spine**: H3 migration forward-compat + unique index access_token.tokenHash + prefix/last4 + ingest resolver.
- Spec Monitoring (B) phải khắc invariant Q2: read-model phủ isolation per-source, KHÔNG phẳng hoá.
- Đóng thread comms/active → resolved sau khi memo locked được consume.

## Blockers / Risks
- H3 migration đụng machines/ingest — bắt buộc forward-compat (resolver tra access_token trước rồi mới drop cột) để không gãy collector đang chạy.
- Artifact gốc nằm trên branch consultant `claude/machines-feature-review-txmEQ`; bản verdict này ở branch CTO `claude/machines-decomposition-cto-BsHYI`. Cần reconcile 2 branch khi merge (chọn bản CTO có verdict).

---
## Phiên 2 (cùng ngày) — GATE plan P0 Access spine
### Đã làm
- Merge `origin/claude/machines-feature-review-txmEQ` vào nhánh CTO (sạch): mang về plan `docs/superpowers/plans/2026-06-07-access-spine-p0.md`, thread gate, và verdict thread đã được consultant chuyển sang `comms/resolved/`.
- Đọc plan (5 task TDD) — honor đủ 4 verdict. Verify từ code: `DELETE /api/machines/[id]` hiện đã là soft-revoke (`tokenHash=null`), không xoá row → D3 không vướng cascade.
- **GATED ✅** với 3 điều kiện: **A1 (bắt buộc)** DELETE phải revoke CẢ HAI đường (null machines.tokenHash + revokedAt trên access_token link) + test ingest-401 hai đường — nếu không = revoke giả; **A2** set `access_token.userId` ngay (POST=creator, backfill=ownerUserId) để fulfill Q2 attribution; **A3** nit sentinel last4 `"----"`.
- Xác nhận 3 D của consultant (D1/D2/D3 đều OK).

### Files changed (phiên 2)
- merge commit + `.serena/memories/comms/active/consultant-to-cto-access-spine-p0-plan.md` (+§CTO GATE).

### Next (consultant)
- Nhúng A1+A2+A3 vào plan → worktree → `executing-plans` task 1→5. Migration db:generate/db:migrate ở HOST.
- Đóng thread gate sang resolved sau khi consume.
