# Checkpoint: cto — 2026-06-08

## What was done (CTO — review/gate/consolidate)
- **Consolidate batch → main:** hoàn tất merge kẹt (P0a resume-spine + f1 write-guard), verify `tsc`+**1117 test**, vá lock musl (`@emnapi` regen-trong-alpine), **build prod Docker** `laam-app:latest`, push origin, `db:migrate` (0008 verified live). Sau đó vá drift PR#6 (**migration 0009** access_token + agent_session.userId — db:push out-of-band, đã commit).
- **Triage QA E2E report (06-08):** verify-not-prose 2 finding. ✅ write-guard PASS · regression · P0a schema/WAL. 🟠 Matte Dark A1 (accent light **2.77:1** fail AA — memo "fine" SAI) / A2 (backdrop-blur leftover) / A3 (chart dark). 🔴 **write-tool crater CONFIRMED** (100%@8→0%@16+).
- **Tool-subsetting (gate connector-write-GA):** gated design (4 gate ✅ + sharpening **fallbackK=f(knee), KHÔNG hardcode-15**) → review spec (R1–R5: cache-content-hash, full-catalog-cache, keep_alive, **false-neg-trade**, flat-metric) → gate plan **1a** (verify gmail_send thật + anchors; Q1 1b=kill-switch-spike, Q2 bỏ 24/40) → deep-check plan (grader multi-tool `missing.length===0` ✅, noCall ✅).

## Files changed (đã commit + push origin/main, HEAD ~8a14fb8 + checkpoint này)
- drizzle/0009 (access_token) · CHANGELOG · package-lock (musl) · QA artifacts (checkpoint/backlog/qa scorecards) · comms (qa-e2e triage, tool-subsetting design/spec/plan gates → resolved) · spec §13 review · confirm-eval-knee plan gate.

## Current state
- `origin/main` synced; Docker image built; DB migrated tới **0009**.
- **Tool-subsetting confirm-eval 1a: CLEAR execute** (consultant T1–T4 agent → user T5 host `npm run eval:scale`).
- **Connector-WRITE-GA CHẶN** trên tool-subsetting + eval-recovery; read-GA đi tiếp.

## Next steps
- Consultant execute 1a (worktree, T1–T4) → **user chạy T5 host** → đọc knee → khoá `fallbackK=knee−margin`/`capK` vào spec §6.
- Rồi plan **1b** (embedding-client + recall@K spike, hard-cases implicit+đa-ngữ BẮT BUỘC) → CTO gate. Cược R4: `miss-rate(recall@K) ≪ crater-rate` — chưa chứng minh.
- **Backlog mở:** Matte Dark A1–A3 fix; P0a behavioral crash-resume (chờ user OK `docker restart`); Access-spine behavior (seed token+collector).

## Blockers / Risks
- Knee chưa đo (T5 host-only). Lock hygiene host-npm11 vs alpine — cân nhắc pin `engines.npm`.
