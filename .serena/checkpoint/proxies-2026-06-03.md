# Checkpoint: proxies (Wave 3 Pkg W3-A) — 2026-06-03

## What was done
- Ported v1 backend proxy endpoints (bin/laam.js) to v2 Next route handlers, all `auth()`-guarded (401 if no session), Ollama via `OLLAMA_URL` (default http://localhost:11434).
- TDD: test-first for each route; 31 tests pass across 7 files.

## Files changed / added (all NEW, uncommitted)
- v2/src/app/api/ollama/models/route.ts (+ .test.ts) — GET → `{ models: string[] }` (NAMES extracted from Ollama /api/tags; 502 on unreachable).
- v2/src/app/api/chat/info/route.ts (+ .test.ts) — GET → `{ model }` from DEFAULT_CHAT_MODEL.
- v2/src/app/api/fetch-url/route.ts (+ .test.ts) — POST SSRF-guarded fetch; exports pure `isBlockedHost`; htmlToText; caps text 12000.
- v2/src/app/api/geocode/route.ts (+ .test.ts) — GET Nominatim search, cache+throttle, UA header.
- v2/src/app/api/reverse/route.ts (+ .test.ts) — GET Nominatim reverse, cache+throttle, UA header.
- v2/src/app/api/route/route.ts (+ .test.ts) — GET OSRM driving, [lng,lat]→[lat,lng], 502 fail-soft.
- v2/src/app/api/nearby/route.ts (+ .test.ts) — GET Overpass by category + Nominatim viewbox fallback, UA header.
- docs/superpowers/plans/2026-06-03-v2-wave3-pkgA-proxies.md — sub-plan.

## Current state
- WORKING. `cd v2 && npx vitest run src/app/api/ollama src/app/api/chat/info src/app/api/fetch-url src/app/api/geocode src/app/api/reverse src/app/api/route src/app/api/nearby` → 7 files, 31 tests pass.
- tsc --noEmit reports NO errors in any of my 7 owned route files.
- Not committed (per instructions).

## Deviations (flagged to TL)
- `/api/ollama/models` returns `{ models: string[] }` (model NAMES) per parent plan line 57 "return model name list" — DIFFERS from v1 which raw-proxies the tags payload. ChatClient (TL) consumes names.
- geo routes keep v1's in-process Map cache + throttle. Tests use distinct query keys to avoid cache cross-talk (fixed one nearby test that collided on cache key — `nearby-test` URL param is NOT part of the cache key, which is lat/lng/q/radius only).

## Next steps
- TL integration: wire these into ChatClient (models dropdown, fetch-url attachments, map geocode/route/reverse/nearby).

## Blockers / Risks
- None. Nominatim/OSRM/Overpass are external free services; routes fail-soft (502/404) as v1 did.
