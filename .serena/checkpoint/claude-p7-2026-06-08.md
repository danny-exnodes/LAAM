# Checkpoint: claude (P7 — editor UX overhaul + #3) — 2026-06-08

## What was done
**Batch A (editor/detail UX) — COMPLETE, all on `main`** (1272 tests green, tsc clean):
- **A1** (`a15ef2f`) — left **Nodes Library** panel (Agent/Connector/Condition/Foreach cards,
  icon+name+desc) replacing the desktop palette row; **click-to-add** + **drag-to-add**
  (canvas onDrop, node-kind MIME); **show/hide/float** (persisted), mirroring the config
  panel. Toolbar regrouped — library + dock/float toggles clustered by undo/redo (the
  float-panel button was stranded). Palette row kept mobile-only. Verified live.
- **A2** (`36a034d`) — run **waterfall** inline when a run expands: pure `waterfallLayout()`
  (offset/width % from step startedAt/finishedAt; running→run-end; seq-sorted) + `RunWaterfall`.
  Verified live (fetch 16ms → summarize 2.83s under workflow() 2.86s).
- **A3** (`480dcfb`) — workflow **name is a link → detail** + **inline rename** ("Đổi tên"
  menu → input → PATCH {name}, optimistic). Fixes the "wall of Workflow mới" clutter.

Studied `vercel-labs/workflow-builder-template` (React Flow + shadcn ButtonGroup grouping +
xyflow Panel + run-logs-with-durations + "Ask AI" bar) — informed A1/A2 + #3's AI bar.

## Files
New: `editor/NodesLibraryPanel.tsx`(+test), `RunWaterfall.tsx`(+test). Modified: `WorkflowEditor.tsx`,
`WorkflowDetailClient.tsx`, `WorkflowsClient.tsx`, `i18n/workflows.ts`.

## Current state / WORKTREE WARNING
- A1/A2/A3 committed to `main` (HEAD `480dcfb`). The shared worktree is on the OTHER session's
  **`feat/landing-page`** branch — so each A-commit went to main via a throwaway worktree
  (`git worktree add --detach … origin/main` → copy files → push HEAD:main), guarded by a
  `git diff main feat/landing-page` divergence check. My A1/A2/A3 changes remain as an
  UNSTAGED overlay in the shared worktree so the dev server shows them.
- Earlier confusion: A1's panel "vanished" because the worktree was on feat/landing-page (no A1)
  after cleanup; restored via `git restore --source=main --worktree`. libraryMode "hidden"
  persisted from testing also made it look gone — now defaults docked.

## Next steps
- **C = #3** AI generate-from-prompt — spec ready (`docs/superpowers/specs/2026-06-08-ai-generate-workflow-design.md`, commit `676f875`).
  NOT started. Large: pure `generate.ts` (buildCatalog/coerceGraph/GRAPH_FORMAT) + `/api/workflows/generate`
  (Ollama `format` + coerce + assertRunnable + retry) + `AiGeneratePanel` (Ask-AI bar) + editor
  apply-with-undo + tests. Live verify needs Ollama running. Best done as a fresh focused pass.

## Blockers / Risks
- Two sessions share one worktree → recurring branch flips. Mitigated by temp-worktree commits +
  divergence guards. If the other session `git add -A` commits, my overlay could land on their branch
  (A1/A2/A3 are safely on main regardless). Recommend each session get its own worktree.
