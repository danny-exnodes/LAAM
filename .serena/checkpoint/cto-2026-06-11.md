# Checkpoint: cto — 2026-06-11

## What was done
- **Audit toàn dự án 7 hạng mục** (7 agent read-only + CTO verify claim): điểm/verdict per-category,
  2 claim P0 của agent bị CTO bác sau khi soi code (OCR-text ĐÃ tới model qua message-prefix
  `route.ts:256`; "vision gap" = images chưa vào payload — POC deferral, P1 không phải P0).
- **Phát hiện vận hành then chốt:** container `laam-v2-app` (prod :3900) đang PUBLIC qua Tailscale
  Funnel với `/api/register` mở + không rate-limit → SEC thành P0 ngay. Tick task CHƯA cài →
  schedule prod không chạy. Không có DEPLOYMENT runbook. Drizzle KHÔNG có index nào.
- **Chiến lược 3 đợt:** R0 code-gate → R1 host/operator → R2 post-release (chi tiết trong transcript;
  bảng R0 8 mục: SEC-1/2, DB-1, UI-1(A1-A3), CHAT-1, I18N-1, TEST-1, OPS-1).
- **R0 IMPLEMENTED** qua workflow 7 agent trong worktree `.claude/worktrees/release-r0-hardening`
  (junction node_modules, KHÔNG npm install, main không nhiễm). Commit **`3374a8f`** trên branch
  `release/r0-hardening`: 46 file, +3560/−123. CTO tự vá thêm: ô inviteCode trên /register (+2 key i18n).

## Files changed
- Branch `release/r0-hardening` (CHƯA merge, CHƯA push): security (REGISTER_MODE/rate-limit/lockout/
  bcrypt12/headers, CSP hoãn R2), drizzle 0010 perf indexes (+snapshot viết tay), A1/A2/A3 fix,
  chat tool-loop error wrap + auto-send, i18n auth dict 28 key + stragglers, docs/DEPLOYMENT.md,
  scripts/{install-tick-task,backup-db}.ps1, README/.env.example/CHANGELOG.
- Main `.serena`: backlog/matte-dark-qa-ui-bugs.md (A1-A3 fixed-on-branch + residual mới), checkpoint này.

## Current state
- Worktree verify: **1380/1380 test (218 file) + tsc sạch** (tăng +43 test từ 1337).
- Main không đổi (chỉ .serena). Prod container vẫn chạy bản cũ (chưa có R0).

## Next steps
1. **User quyết:** merge `release/r0-hardening` → main; Funnel :3900 giữ public hay tailnet-only;
   flip 9 write-tool tier-low?
2. Sau merge (host): `npm run db:generate` verify "no changes" (snapshot 0010 viết tay) →
   `db:migrate` (0010) → rebuild image → set env (REGISTER_MODE=invite + REGISTER_INVITE_CODE,
   WORKFLOW_TICK_SECRET) → restart container (nạp luôn gmail allowlist) →
   `scripts/install-tick-task.ps1` → lịch backup-db.ps1 → OAuth Console (checklist trong DEPLOYMENT.md).
3. R2 backlog: scan-cache M1/SSE-cache M2, collector retry, bảng events + filter máy/owner,
   vision images, Matte Dark rollout 19 trang, Streamdown flip, cancel-run, CSP, v1-unported Search.

## UPDATE — cùng phiên: MERGED (user authorized)
- ✅ Commit serena docs `2df7eb8` → **merge `release/r0-hardening` → main = `8614ecd`** (no-ff, 0 conflict).
- ✅ Verify TRÊN MAIN sau merge: **1380/1380 test + tsc exit 0**.
- ✅ Worktree release-r0-hardening đã remove an toàn (rmdir junction trước — node_modules thật nguyên vẹn).
- CHƯA push origin (user chưa yêu cầu). Worktree cũ `workflow-high-blast-mechanism` (PR #8, đã merge) vẫn còn — prunable, chờ user.
- Đã đưa user lệnh rebuild dev env (db:generate verify → db:migrate 0010 → restart `npm run dev` :3100).

## Blockers / Risks
- Snapshot drizzle 0010 viết tay — verify `db:generate` no-change trên host (bước 1 của dev rebuild).
- Rate-limit/lockout in-memory single-process (restart mất state — chấp nhận cho 1 box).
- REGISTER_MODE giá trị lạ → fail-closed như 'closed' (quyết định an toàn, đã comment).
- App đang public với register mở CHO TỚI KHI deploy R0 (hoặc tắt Funnel) — đôn đốc user.

## UPDATE 2 — R1 host + R2 post-release EXECUTED (user: "tiếp tục đến hoàn thành chỉn chu")
### R1 (host/deploy) ✅
- migrate 0010 (db:generate "no changes" → migrate → 6 index live trên Postgres).
- `.env` prod: REGISTER_MODE=invite + REGISTER_INVITE_CODE (random) + WORKFLOW_TICK_SECRET (random).
- Rebuild image + recreate container :3900 → R0 rồi R2. Verify: /login 200 + 5 headers, register 403 không-mã, tick 200, **gmail_send active** (allowlist nạp lúc restart).
- Tasks: LAAM-workflow-tick (1') + LAAM-db-backup (02:00, ret14, smoke 766KB) + LAAM-host-metrics (:47600, logon-trigger vì boot cần Admin) — đều Running.

### R2 (code, branch feat/r2-postrelease → merge `fa69046`) ✅
- 7 agent song song + Docs + Verify + **adversarial review** (approve-with-nits, 8 finding).
- CTO tự xử 8 review-finding (agent fix bị rate-limit giữa chừng, làm 4/8; CTO làm nốt 4 + tự kiểm 8): P1 ingest chunked-cap, P1 SSE client-death cleanup, void-broadcast catch, finalize-race conditional, cancel 'resumable', stuck-threshold thread, bottom-nav flex-1, README.
- **Bắt lỗi giấu (Rule 12):** collector test (.mjs shebang) không parse được trong rolldown → file FAIL bị nuốt (1463 vs 1470). Fix: tách helper → `collector/retry.mjs` không-shebang. Báo cáo W2 "pass" là SAI.
- Verify cuối trên main: **1470/1470 test (228 file) + tsc sạch**. R2 image deployed, container healthy, /search + /api/config 307→login (auth-gated đúng, không leak).

### Commits main (chưa push): 8614ecd(R0) → 1ed8b99(R1 ops) → fa69046(R2 merge) → a0f99c4(collector fix).

## UPDATE 3 — Chat tool-selection review + QW (user: "agent ngu khi chọn tool/trả lời")
- **4-agent review + eval ĐO THẬT** (qwen3-vl:8b). Reviewer A đào ra: "write 0%@16+ crater" = ARTIFACT eval-probe ĐÃ BỊ team BÁC 06-08 (verify trực tiếp `comms/active/consultant-to-cto-1a-prime-result.md`). → KHÔNG dựng embedding-subsetting (user duyệt). Decision: [[chat-tool-selection]].
- Eval k=3 baseline: agent KHÔNG ngu chung — read/args/restraint/term 92-100%; yếu thật chỉ ở web-research-loop (2/3) + write confab (runtime-guarded).
- Ship QW-1/2/3/5 (merge `86dc753`) → đo k=6: **REGRESS** write-selection (write-intent sel 3/3→2/6→0/6). Chẩn: QW-5 few-shot neo demo_create_task + QW-1 grouping. **REVERT QW-1+QW-5** (`7443918`), giữ QW-2 (desc) + QW-3 (web_read nudge).
- **Eval k=6 cuối (flat + QW-2 + QW-3):** sel **100%** / args **100%** / restraint+term+block **100%** / **web-research 2/3→6/6** (QW-3 thắng) / **write-selection 0/6(few-shot)→6/6(flat)** (revert cứu, không nhiễu) / write-intent 3/6 = confab (write-claim-guard chặn ở prod). Evidence: `.serena/qa/eval-2026-06-11-{after-k3,qw-with-fewshot-k6,final-flat-qw2qw3-k6}.md`.
- **BÀI HỌC (process miss của tôi):** merge prompt-change TRƯỚC khi đo model = sai → regress → revert. write-intent-trello là probe write DUY NHẤT + noisy (20-100%) → đừng tune prompt trên nó. Backlog: dựng ≥3 write-probe args-hợp-lệ k≥10 trước khi tune write-selection. **Prod CHƯA redeploy QW → user thật KHÔNG bị ảnh hưởng suốt vòng lặp.**

### Commits main (chưa push): …→ `86dc753`(QW merge) → `7443918`(revert QW-1/5).

## UPDATE 6 — Bug "Lỗi server" khi upload PDF (prod) — chẩn đoán + redeploy
- User báo upload PDF → "Lỗi server" trên prod. Log prod: **NUL-byte Postgres crash** (`0x00`, code 22021) + khối PDF nhị phân bị insert = ĐÚNG bug `stripNul` đã sửa (`61caae7`).
- **Gốc:** prod container chạy **image 7h tuổi** (bản R2 sáng nay) — CHƯA có 20 commit fix (NUL-strip, PDF feature, truncation/sort, QW). "Rebuild" của user không vào container đang chạy.
- **PDF support thật:** `72706df` text-layer→OCR→vision (pdfjs-dist@6, worker `new URL`). **`next build` trong Docker QUA** → worker đóng gói OK ở production (cổng pdfjs đã verify gián tiếp qua build).
- **Lockfile drift lần 4:** `npm install pdfjs-dist` trên host (npm11) rớt `@emnapi/runtime`+`@emnapi/core` → `npm ci` alpine gãy → build đầu FAIL. Fix: regen lock trong `node:22-alpine` (`--package-lock-only`, host node_modules nguyên) — commit `efb4013`. **RULE: sau mọi npm install trên host PHẢI regen lock trong alpine trước Docker build.**
- ✅ **Rebuild + redeploy prod** (image mới, container healthy, /login 200+headers). NUL crash hết (stripNul + client PDF mới). PDF feature LIVE. HEAD=`efb4013`.
- **Chờ:** user test lại upload PDF trên prod `:3900` (text PDF + scan PDF) — đây là smoke-test browser cuối cho pdfjs runtime (build đã qua).
### Chờ user: redeploy prod (`docker compose build app && up -d app`) để QW-2+QW-3 (web_read nudge + write desc) lên live?

## UPDATE 4 — Scale write-eval ĐỊNH LƯỢNG (user chọn "dựng bộ write-eval đa-probe")
- Phát hiện: bộ này PHẦN LỚN ĐÃ CÓ (`suite.scale.eval.ts`, CTO 1a′). Việc thật: **+2 bare-write probe** (github/demo args-hợp-lệ, commit `5270949`) → 5 họ tool, + **CHẠY k=10** trên build hiện tại.
- **Kết quả (`.serena/qa/eval-scale-2026-06-11.md`):** bare-write (trello/gmail/gcal/github/demo) = **100% mọi N tới 16 tool** → crater CHẾT HẲN, KHÔNG cần embedding. Gap duy nhất: **ctx-web-write (web_search→trello) @16 = 30%** = lỗi chuỗi multi-step (cùng họ QW-3), không phải write-capability.
- Trạng thái cuối chat-review: GIỮ QW-2+QW-3, REVERT QW-1+QW-5; write-selection chứng minh 100%@scale; embedding/đổi-model HOÃN đúng. HEAD main = `5270949` (chưa push).
- **Chờ user:** redeploy prod QW-2+QW-3? / vá ctx-web-write chain (cần ≥2 probe trước)? / trello name→idList (QW-4)?

## UPDATE 5 — Bug prod "Dữ liệu bị cắt ngắn" (chain nhiều tool) — FIXED
- Ca: "top 5 agents tốn tiền nhất → gửi mail" → agent list-all → tràn 8192 → cắt → bó tay. Lỗi gốc: `boundOutput` slice JSON HỎNG + thiếu query "top-N by cost".
- **Fix (deterministic, commit `b78f78c` + `fe80615`):** (1) `boundOutput` phục-hồi-được (mẫu hợp lệ + total + note thu-hẹp — vá MỌI connector); (2) `laam_list_agents` thêm `sort=recent|cost|tokens`. Eval k=6: top-agents-cost **6/6**, agent-detail **6/6**, mọi chiều sel/args **100%**. 1481 test + tsc sạch.
- **Bài học lặp (lần 2):** guidance trong **description TOOL** = empirical (câu "đừng list" làm agent-detail 6/6→0/6); chuyển guidance vào **param enum** → cả hai 6/6. Quy tắc ghi vào [[chat-tool-selection]].
- HEAD main = `fe80615` (chưa push). Backlog: audit list-vs-query 7 connector; redeploy prod.

## UPDATE 6 — PDF upload "không đọc được tệp" trên iPhone/máy khác (KHÁC host) — FIXED + DEPLOYED
- Triệu chứng phân biệt (user phát hiện): upload PDF **từ chính host (Chrome 149) CHẠY**, từ **iPhone / laptop khác KHÔNG**. Pipeline PDF chạy 100% client-side ⇒ lỗi đặc thù **năng lực trình duyệt**, không phải file/server.
- **Chẩn đoán deterministic (không đoán):** verify cạn server-side trước (image fresh 16:50, client+dict mới serve, worker+lib chunk 200 qua Funnel đúng MIME, tesseract live, no CSP, no service-worker, API getDocument/render/workerSrc khớp v6). Còn lại = throw runtime bị **`catch {}` nuốt** (ChatClient:536). Grep build: **pdfjs v6 modern dùng `Promise.withResolvers` ở CẢ main LẪN worker** — API này cần Safari **17.4+** / Chrome 119+. iPhone Safari <17.4 + laptop cũ thiếu → ném khi nạp pdfjs → nuốt → "không đọc được tệp". Host Chrome 149 có nên chạy.
- **Fix (commit `04efca4`, đã build+deploy):** chuyển sang **pdfjs `legacy/build`** (Babel + core-js polyfill) cho cả dynamic import lẫn worker URL. Legacy tự polyfill withResolvers trong **cả hai scope**. (`exports`=undefined nên import subpath legacy tự do; thêm ambient `pdfjs-legacy.d.ts` cho types; 0 dep mới — legacy có sẵn trong pdfjs-dist.) **Fail-loud (Rule 12):** catch giờ lộ `err.name+message` ra model+console thay vì nuốt.
- **Verify (gold-standard, KHÔNG cần iPhone):** trên host browser xoá `Promise.withResolvers` → nạp legacy build → **nó tự thêm lại + getDocument+worker chạy end-to-end** (`extractedText:"Hello LAAM probe"`). 55 test chat + tsc sạch. Image mới: polyfill `withResolvers:function` có trong worker asset (hash mới `02o-ksyisub91`, 1.30MB > modern 1.25MB) + client chunks. Deployed: container healthy, worker 200 qua Funnel. HEAD main=`04efca4` (chưa push).
- **Chờ user:** test thật upload PDF **trên iPhone** (xác nhận real-device cuối — phần duy nhất tôi không tự chạy được). Backlog vẫn: audit list-vs-query 7 connector; push origin; (tùy) .docx qua mammoth.

## UPDATE 7 — PDF parse Ở SERVER + attachment preview/persistence — DONE + DEPLOYED
- **Diễn biến tiếp:** sau legacy build, iPhone Safari VẪN lỗi nhưng ĐỔI message ("không thể đọc do lỗi hệ thống"). Lấy lỗi thật **từ DB** (fail-loud reason persist trong `chat_message.content` qua withAttachments): **`TypeError: undefined is not a function (near '...t of e...')`** — cú pháp `(near …)` = WebKit/Safari. Tức legacy qua được withResolvers nhưng pdfjs v6 vướng **API Safari-only KHÁC**. Client-side pdfjs = whack-a-mole theo trình duyệt.
- **User chốt:** parse PDF **ở server**. **PART 1 (commit `5067ac8`, deployed):** POST `/api/pdf` (multipart) — poppler `pdftotext -layout` → `pdftoppm`+tesseract OCR → ảnh trang cho vision. Tái dùng `runPdfTiers` (chuyển sang `pdf-tiers.ts`, tiêm primitive server ở `pdf-server.ts`). Dockerfile += `poppler-utils`. **XOÁ** client pdfjs (`pdf.ts`,`pdfjs-legacy.d.ts`) — `pdfjs-dist` giờ unused (để lại trong package.json, cleanup lock-regen sau để tránh musl). Verify end-to-end qua Funnel (authed): text→đúng chữ, OCR→đúng chữ, scan→vision JPEG <2MB. **Chạy y hệt mọi thiết bị** (server-side). poppler 25.12 + tesseract 5.5.2 trong image.
- **PART 2 (commit `79d7a63`, deployed):** attachment preview + **persist qua reload**. Cột `chat_message.attachments` jsonb (**migration 0011**, đã `db:migrate` vào DB chung) chứa `[{name,kind,mime,size,preview}]` (thumbnail data URL, KHÔNG file gốc). Type chung + sanitizer trust-boundary `attachment-meta.ts`. `/api/chat` persist sanitized; `/api/conversations/[id]` trả về; ChatClient map lại lúc reload. `/api/pdf` trả `thumb` (pdftoppm -r 60 trang 1); ảnh → `makeThumb` canvas cơ bản (bail nếu không có canvas → jsdom an toàn). Component `AttachmentPreview` (thumb/icon + name + size) render ở message bubble (persist) + composer chip (+ nút xoá). Verify: POST persist → reload trả preview; preview non-`data:` độc hại bị strip. **1515 test + tsc sạch.** /chat reload không console error.
- **HEAD main=`79d7a63`** (CHƯA push). Test conv verify đã xoá sạch (DB). **Chờ user:** smoke UI thật (upload ảnh/PDF → thấy thumbnail ở chip + bong bóng, reload vẫn còn) + iPhone. Backlog: push origin; gỡ `pdfjs-dist` (regen lock alpine).

## UPDATE 8 — .docx đọc Ở SERVER (`35bd7c4`, deployed)
- .docx trước bị đọc nhị phân (rác). Giờ parse server như PDF: `POST /api/docx` → `unzip -p word/document.xml` → `docxXmlToText` (gộp `<w:t>` theo `<w:p>`, tab/br, text cell bảng, decode entity, cap 50k). Dockerfile +=`unzip` (cực nhẹ, **KHÔNG npm dep** → không đụng lockfile/musl — chủ ý, KHÔNG mammoth/pandoc). Client: nhánh `isDocxFile` → POST. `.doc` cũ (không phải zip) vẫn rớt về thông báo binary-unsupported.
- Verify: parser 7 unit test; `unzip -p` bóc đúng `word/document.xml` (forward-slash, như docx thật) từ zip spec-chuẩn trong image runtime. 1522 test + tsc sạch. **HEAD main=`35bd7c4`** (chưa push).
