# Checkpoint: claude-sp2 — 2026-06-05

Vai trò: orchestrator SP-2 "Actions & safety" (Agent Harness). Arc: brainstorm → spec → plan → **IMPLEMENT** (inline TDD, worktree riêng).

## What was done
- Boot Protocol + khảo sát code SP-1 (`src/lib/agent/*`) + connectors. Phát hiện chốt: write surface = **đúng 1 tool** `trello_create_card`; connector KHÔNG qua `guard()/boundOutput` (lỗ hổng L4); Trello nhét cred vào query string (redact thật); tool-loop chạy server-side TRƯỚC stream ⇒ gate phải suspend/resume.
- Brainstorm → user chốt **stateless signed token** + **backend/wire-contract only**. Spec + plan viết; **lead APPROVED cả hai** (re-review §6 đạt; 2 pre-flight đã làm: `lang→readLang`, frame qua `encodeFrame` shared `frames.ts`).
- **IMPLEMENT 9/9 task xong** (worktree `feat/agent-harness-sp2`, inline TDD, 1 commit/task `4c7cf76`→`2b5b3e0`).

## Files (code ở worktree; spec/plan/Serena ở main — live shared)
- Mới: `src/lib/agent/safety/{redact,policy,token,preview,audit,gate,resume}.ts` (+tests) · `src/lib/chat/frames.ts` (+test — land-first cho SP-4, chỉ `encodeFrame`+type).
- Sửa: `src/app/api/chat/route.ts` (+test) — union body, suspend/confirm, `streamOllama` extract, `SEP=String.fromCharCode(0x1e)`.
- Docs/Serena (main): spec, plan, decision `agent-harness-sp2-actions-safety`, backlog `agent-harness-sp2-fe-confirm`, comms (lead resolved + sp4-frames + reviewer).

## Current state
- **Implementation COMPLETE + verified:** `npx vitest run` = **451 pass** (94 files; baseline 415 + 36 mới); `npx tsc --noEmit` = **clean (exit 0)**. Working tree sạch.
- **Zero đổi hợp đồng SP-1** (gate = wrapper `withSafety` bọc dispatch; `PendingWriteSignal` throw xuyên `runToolRounds`). **Không schema** (dùng `audit_log`).
- Phase 6 review thread MỞ: `comms/active/sp2-to-reviewer-code-review.md`.

## Next steps
- Reviewer/lead review implementation → address feedback → **Phase 7 finish** (merge `feat/agent-harness-sp2`→main, hoặc PR).
- Optional: `next build` + manual smoke (user chạy server+Ollama — agent-ops-rules). FE confirm card = handoff `agent-harness-sp2-fe-confirm` (cần SP-4 frame-router `splitFrames`).

## Blockers / Risks
- Race nonce tồn dư (no unique index — threat model nội bộ, defer fix bền SP-3; đã nêu, không giấu — Rule 12).
- `frames.ts` + `route.ts` co-touch SP-4 (additive) → rebase cẩn thận khi merge thứ tự. Render card chờ SP-4 `splitFrames`.
- Path discipline: code ở worktree, Serena/docs ở main. node_modules junction từ main.
