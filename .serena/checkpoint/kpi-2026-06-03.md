# Checkpoint: kpi (W2-A) — 2026-06-03

## What was done
- Wave 2 Package W2-A: 5 pure `"use client"` dashboard widgets + tests (TDD), conforming to the LOCKED prop contracts.
- KpiGrid({totals}): 11 KPI cards (sessions/running/idle/done/messages/toolCalls/subAgents/tokensIn/tokensOut/cost/avgDur); num()/usd() + local fmtDur(ms→"9m 30s").
- StatusDoughnut/ModelDoughnut/BranchDoughnut: share a `Doughnut.tsx` helper (recharts PieChart + an EXPLICIT legend list we render — deterministic in jsdom). Status relabeled via dash.st.*, model via shortModel(), branch raw. Zero slices dropped; empty record → empty state.
- Heatmap({heatmap}): 7×24 CSS grid, opacity=value/max (floor 0.08), sparse hour header, day labels (Sun-first), per-cell title, low/high legend, empty state. Ported from public/dash-heatmap.js.

## Files changed
- Added: src/components/dashboard/{KpiGrid,StatusDoughnut,ModelDoughnut,BranchDoughnut,Heatmap,Doughnut}.tsx + 5 *.test.tsx (Doughnut is a shared internal helper, no test of its own — covered via the 3 doughnut suites).
- Added plan: docs/superpowers/plans/2026-06-03-v2-wave2-pkgA-kpi.md
- EDITED (ADD-only, all 3 langs) src/i18n/dictionaries/dashboard.ts: dash.kpi.subAgents, dash.kpi.cost, dash.kpi.tokensIn, dash.kpi.tokensOut, dash.chart.empty. No existing keys changed.

## Current state
- `npx vitest run` on my 5 files: 18/18 pass. `tsc --noEmit` clean on my files (fixed a recharts 3.x Tooltip formatter typing).
- Left uncommitted for TL review. Did NOT run full npm test / build / commit (per scope).

## Next steps (TL)
- TL builds DashboardClient.tsx + page.tsx composing these + B's charts + C's tables.

## Blockers / Risks
- None. Note for TL: the 5 ADDED dict keys (flagged above). recharts renders 0×0 in jsdom → doughnut tests assert on our own legend list, not chart svg.
