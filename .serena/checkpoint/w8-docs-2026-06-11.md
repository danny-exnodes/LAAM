# Checkpoint: w8-docs — 2026-06-11

## What was done
- CHANGELOG.md `[Unreleased]`: thêm 3 section R2 post-release (Đã thêm / Hiệu năng / Đã sửa)
  cover đủ W1–W7: scan-cache + SSE shared snapshot, ingest 413 + collector retry,
  chat Vision (cap 2×2MB), workflow cancel + toast, /api/config + LAAM_STUCK_MIN + lọc máy,
  Search (/search + /api/search), contrast residual (--accent-fill, chart series palette).
  Đối chiếu `git status`/`git diff --stat` với main — chỉ ghi thứ có trong diff.
- .env.example: thêm block `LAAM_STUCK_MIN` (comment vi ngắn, default 10, clamp 1..120,
  phân biệt với PROACTIVE_STUCK_MIN) ngay trên section "Nguồn dữ liệu giám sát".
- README.md: KHÔNG sửa — không có danh sách tính năng/route để chèn /search
  (Layout = snapshot thư mục thời Phase 1, Status = lịch sử phase). Ghi rõ theo đề bài.

## Files changed
- CHANGELOG.md (sửa — 3 section mới trong [Unreleased])
- .env.example (sửa — thêm LAAM_STUCK_MIN)
- .serena/checkpoint/w8-docs-2026-06-11.md (mới)

## Current state
- Docs khớp diff thực tế của worktree. Connector/action picker của W4 KHÔNG ghi changelog
  (W4 xác nhận đã có sẵn trong base, không nằm trong diff R2).

## Next steps
- Orchestrator quyết: CHANGELOG dòng cũ trong mục release 2.1.0 vẫn ghi
  "secondary 11.4/6.7:1" (W5 flag) — sửa release-notes đã phát hành hay giữ lịch sử.
- CLAUDE.md "Not yet ported from v1" + danh sách Routes đã drift (Search, /api/config
  đã port; /search, /api/search, /api/config chưa có trong list) — ngoài scope W8.
- README/docs/DEPLOYMENT.md chưa có bảng env nhắc LAAM_STUCK_MIN — cân nhắc khi
  cập nhật runbook.

## Blockers / Risks
- Không — docs-only, không test nào pin nội dung CHANGELOG/.env.example (đã grep).
