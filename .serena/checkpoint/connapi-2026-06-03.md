# Checkpoint: connapi — 2026-06-03

## What was done
- W4-A connector API routes (TDD, against LOCKED `@/lib/connectors` signatures).
- GET /api/connectors → auth() → `{ connectors: list(userId) }`; 401 if no session.
- POST /api/connectors/:id/:action → auth(); routes connect/disconnect/test to
  framework fns; unknown action → 400; never echoes submitted secrets.

## Files changed
- CREATE v2/src/app/api/connectors/route.ts
- CREATE v2/src/app/api/connectors/route.test.ts
- CREATE v2/src/app/api/connectors/[id]/[action]/route.ts
- CREATE v2/src/app/api/connectors/[id]/[action]/route.test.ts
- CREATE docs/superpowers/plans/2026-06-03-v2-wave4-pkgA-connapi.md (plan)

## Current state
- `npx vitest run src/app/api/connectors` → 2 files, 8 tests pass.
- tsc: no errors in my two route files (grep -i connectors on tsc output = clean).
- NOT committed (per instructions). Uncommitted for TL review.

## Next steps
- TL integration: full `npm test` + `npm run build` after F+C+U+T land.
- Framework agent's real index.ts bodies replace stubs — my routes call them as-is.

## Blockers / Risks
- None. Routes depend only on locked framework signatures; live behavior depends
  on framework (W4-F) + connectors (W4-C) being implemented + DB migration on host.
