# Checkpoint: cto — 2026-06-16

## What was done (this session)
- **CTO progress audit** (5 parallel agents) → reconciled major **memory drift**: project is at v2.1.0+ (durable workflows, world-tools, SP-4 merged); INDEX was stale (claimed v2.0.0 / "not built"). Verified against git, not memory.
- **Workflows cluster** deep-audited (grounded in code): ~95% complete, GA-ready. No critical bugs open. Found 1 real gap (off-boarding → schedule auto-disable).
- **Custom Agents** — diagnosed "already built, just unreachable"; developed reach beyond workflows → **merged `e296852`, pushed origin, live E2E PASS** (chat persona w/ `E2E-PERSONA-OK` marker, workflow discoverability link, clone + use-in-chat deep-link). Test artifacts cleaned up.
- Fixed pre-existing `main` env breakage (missing `pg` from Node 24 lockfile bump) via `npm install` (user-authorized).

## Current state
- `main` = `e296852`, in sync with origin (as of merge). tsc clean, **1987 tests pass**.
- Custom-agents feature: DONE + E2E-verified (supersedes `custom-agents-dev-2026-06-15.md` "awaiting E2E").

## Next steps — cluster review (continue ORDERLY, in this order)
1. **Connectors / OAuth** ← NEXT (start here)
2. Chat / MCP / Vision / OCR
3. Security / RBAC / Access
4. Monitoring / Machines / Collector
5. Matte Dark page rollout

## Open items found so far (parking lot — confirm/expand during cluster audits)
- 🟡 **OCR dead on host** — composer shows "OCR chưa sẵn sàng trên máy chủ"; tesseract missing on dev host (host-ops install, needs admin). → Chat cluster.
- 🟡 **Off-boarding gap** — disabled user's `workflow_schedule` rows keep firing; fix = disable schedules in user-disable tx OR `owner.disabledAt` check in tickClaim (~half-day). → Workflows.
- 🟡 **Pre-BYOK security** — per-user HKDF crypto + SSRF DNS-pin on MCP client. → Security cluster.

## Notes / gotchas
- E2E: drive the **:8443 Tailscale origin** (authed session), NOT localhost:3100 (redirects to :8443/login). See decision `auth-multihost-dev-env`.
- Use element `ref` clicks (not pixel coords) in Chrome MCP — screenshot is downscaled vs page CSS pixels.
- `npm-destroys-worktree-junction` trap recurs on this repo (pg/emnapi) — verify deps on main after worktree/Node work.
