# Checkpoint: cto — 2026-06-16

## What was done (this session)
- **CTO progress audit** (parallel agents) → reconciled major **memory drift**: project is v2.1.0+ (durable workflows, world-tools, SP-4 merged); INDEX still reads v2.0.0/"not built". Verified vs git.
- **Custom Agents** — diagnosed "built but unreachable"; developed reach beyond workflows → merged `e296852`, pushed, **live E2E PASS** (chat persona, workflow discoverability, clone/use-in-chat). Cleaned up test artifacts.
- **Full cluster review** (grounded-in-code), all 7 clusters: Workflows · Custom-Agents · Connectors/OAuth · Chat/MCP/Vision · Security/RBAC/Access · Monitoring/Collector/Machines · Matte-Dark.
- **Fixed off-boarding gap** (`ce09496`, TDD): user-disable now also disables their `workflow_schedule` rows → cron stops firing with their live connector creds. Closed a hole found across Workflows + Connectors audits. 1988 tests green, tsc clean.
- Fixed pre-existing `main` env breakage (missing `pg` from Node 24 lockfile bump) via `npm install`.

## Cluster verdicts — ALL GA-ready, no functional bugs
- Workflows 🟢 ~95% · Custom-Agents 🟢 done+E2E · Connectors 🟢 ~92–100% · Chat 🟢 code-complete · Security/RBAC 🟢 strong (all 34 mutations gated) · Monitoring/Machines 🟢 complete · Matte-Dark 🟢 **shipped & guarded**.

## Key corrections (vs earlier assumptions)
- **Matte-Dark is COMPLETE by design** — token-repoint lever (legacy `dark:bg-neutral-900` == `--surface-1`). "0/24 pages import MatteCard" is intended; primitives are opt-in. A1 contrast 2.77→5.57:1 fixed, test-guarded. NOT unfinished.
- Off-boarding gap spanned 2 clusters; one fix (this session) closes both.

## Current state
- `main` HEAD = `<push commit>` (off-boarding fix + checkpoint + backlog). Pushed to origin this session.
- Remaining work = a short security-hardening backlog (see `backlog/platform-cluster-review-remaining.md`).

## Next steps (new session)
1. Reconcile stale **INDEX.md** to v2.1.0+ reality (still claims v2.0.0).
2. Tackle backlog by priority: recipient-gate format-aware (~2-3h) → SSRF DNS-pin (~1-2d) → per-user HKDF (~2-3d, gates BYOK).
3. Ops: install tesseract on dev host (OCR); operator Zalo/Workspace actions.

## Blockers / Risks
- None functional. The hardening items gate FUTURE features (BYOK, messenger-writes-in-workflows), not current ones.

---

## Session 2 (continuation) — INDEX.md reconciliation
- **Done:** Reconciled stale Serena boot-knowledge to current reality. Grounded version on `package.json`/CHANGELOG = **v2.4.1** (+`[Unreleased]`) — NOT the loose "v2.1.0+" from Session 1 (Rule 13: trusted code over my own prior regurgitation).
- **Edits (memory only, not code):**
  - `memories/INDEX.md`: added top `## Trạng thái hiện tại (2026-06-16)` source-of-truth block (v2.4.1, 7 clusters GA, parity achieved, remaining hardening backlog, latest checkpoint); demoted `[v2-parity-gap]` line to `(LỊCH SỬ — parity ĐÃ ĐẠT)`; refreshed `[v2-app]` pointer; renamed false "Trạng thái hiện tại (2026-06-03)" heading → `(ảnh chụp lịch sử)`.
  - `memories/services/v2-app.md`: added `Refresh 2026-06-16` banner; fixed dev port `:3000`→`:3100` (verified vs `package.json`); demoted Wave status block to historical heading.
- **Verified:** grep confirms a single `Trạng thái hiện tại` heading; residual `chờ test runtime` strings now sit only under dated historical headings.
- **NOT committed** (no user request to commit; working tree change = these memory files).
- **Note for later (out of scope, surfaced not acted):** `CLAUDE.md` also says "v2.0.0" while `package.json`=2.4.1; CHANGELOG `[Unreleased]` may not yet list the `ce09496` off-boarding fix or `e296852` custom-agents-reach. Both are doc-sync follow-ups.

## Next steps (unchanged, INDEX item now ✅)
1. ✅ ~~Reconcile INDEX.md~~ — done this session.
2. Backlog by priority: recipient-gate format-aware (~2-3h, **needs CTO sign-off to flip**) → SSRF DNS-pin (~1-2d) → per-user HKDF (~2-3d, gates BYOK).

---

## Session 3 (continuation) — security-hardening backlog cleared → PR #9
- **User: "clean up all backlog, report after finish."** Confirmed 3 decisions (flip the 3 writes; single integration branch + PR; include HKDF). Did ALL open CODE items TDD in an isolated worktree (`worktree-harden-backlog-security`, node_modules junction → no npm install), one commit per item:
  - `3c3216b` recipient-gate format-aware + flip slack/whatsapp/zalo (per-format allowlists, fail-closed default)
  - `2beb07d` SSRF DNS-pin (deps-free connect-time resolve+validate)
  - `4d3f2f5` defense-in-depth (tool-count cap, bounded MCP error, collector jitter)
  - `9b1b4cf` per-user HKDF (connector + MCP authToken; lazy migration)
  - `cb5d802` doc-sync (CLAUDE.md v2.0.0→v2.4.1+, CHANGELOG Security entry)
- **Adversarial verify** (Workflow, 5 skeptics) caught **2 real bugs** → fixed `06e2ec4`: **CRITICAL** SSRF IPv4-mapped-IPv6 hex bypass (my tests fed dotted form `new URL` never produces — Rule 9 miss), and unbounded MCP toolName defeating the error bound. Recipient-gate/HKDF/completeness: clean.
- **2037 tests green, tsc clean.** Pushed → **[PR #9](https://github.com/danny-exnodes/LAAM/pull/9)**, NOT merged (user reviews/merges).

## Current state (end of Session 3)
- Security backlog code items: ✅ in PR #9 (pending merge). Residual follow-ups (NOT blocking) tracked in `backlog/platform-cluster-review-remaining.md`: SSRF undici-pin TOCTOU · HKDF master-secret (KMS/DPAPI/BYOK) · NAT64 embedding.
- **Operator action to USE the flipped writes:** set `WORKFLOW_{SLACK,WHATSAPP,ZALO}_ALLOWLIST` (else fail-closed).
- ⚠️ Uncommitted on `main` working tree (memory only, NOT code): this checkpoint + `INDEX.md`/`v2-app.md` reconciliation (Session 2) + this backlog update. User decides whether to commit memory.
