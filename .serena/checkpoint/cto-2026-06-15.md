# Checkpoint: cto — 2026-06-15

## What was done
- CTO progress audit via 5 parallel read-only Explore agents (Build/Release, Connectors/OAuth, Harness/Workflow, Chat/MCP/Redesign, Security/Access).
- Reconciled a conflict between agents and verified ground truth in git (Rule 7 + Rule 12).

## Verified ground truth (git, not memory)
- **HEAD = `54eeaa1`** on `main` (stale session snapshot showed `c7c36e1`).
- `feat/batch2` (security hardening, `94b4a08`) and `feat/admin-key` (v2.4.1, `e422ba7`) ARE merged into main — their worktrees are prunable leftovers, not pending work.
- Migrations `0000–0015` present (incl. 0012 disabled_at, 0013 notification, 0014 createdByUserId, 0015 custom_agent).
- 3 live RBAC holes (decorative RBAC / cross-user SSE leak / off-boarding token) = **PATCHED & merged**.
- Connectors: 10 connectors, **43 tools / 15 writes** (memo said 42/14 — minor drift).
- Chat P1–P4 (quicktools/MCP/custom-agents) MERGED at `ac76146` (memory still says "CHƯA merge").

## Current state
- Working: prod `:3900` (R0+R1+R2). Security hardening, OAuth multiprovider, quicktools/MCP/agents all on main.
- ⚠️ CORRECTION (see Update below): SP-4 FE, World-Tools, and Workflow are NOT "in flight" — they are MERGED (v2.1.0). My first-audit "in flight" claim was wrong; it trusted stale memory.
- Uncommitted: Node 24 + emnapi (Dockerfile/package.json/lock), CHANGELOG, .serena docs.

## Next steps
- Decide: push `main` (~4 commits ahead of origin, incl. security fixes) — currently unpushed.
- Commit Node 24 upgrade + redeploy decision (prod still Node 22).
- Refresh INDEX.md (stale HEAD + "CHƯA merge" lines).
- Prune 3 merged worktrees (batch2, adminkey, workflow-high-blast-mechanism).

## Blockers / Risks
- Pre-BYOK security backlog: per-user HKDF crypto + SSRF DNS-pin (not blocking current features).
- Connector recipient-gate email-only → Slack/WhatsApp/Zalo writes stay fail-closed until format-aware.
- Memory drift could mislead next session until INDEX refreshed.

## Update — planning session (verified vs git/files)
- 🔴 **Major finding: Serena memory is badly stale.** INDEX implies v2.0.0 / "Workflow not built" / "SP-4 gated" / "World-Tools unmerged". Reality (verified by file reads + `git log -1` on cited commits):
  - `578f0ae Merge SP-4` → SP-4 FE DONE (`ToolTrace.tsx`, `ConfirmCard.tsx`, `frames.ts`, `trace.ts` all present+tested).
  - `95365ca chore(release): v2.1.0 — Durable AI Workflows, Gmail-send, World-Tools` → World-Tools merged (`web/`,`util/`, SearXNG in docker-compose); Workflow built FAR beyond A0 (40-file `src/lib/workflow/`: engine, scheduler cron/recurrence/tick, resume, idempotency, templates, blast-gate, React-Flow editor, AI-generate).
  - `73e78b8 Merge FE confirm-card` → SP-2 write-gate UI DONE.
- Lesson: Round-1 audit agents trusted memory and mis-reported; Round-2 + direct git verify corrected it. **Always verify "merged/not-built" claims against files (Rule 13).**

## Revised plan (no code; user chose "prepare plan only")
- **Track D (do first, docs-only, S):** reconcile INDEX/memory to v2.1.0+ reality — stops misleading future sessions.
- **Track A (real engineering, isolated worktree):** SSRF DNS-pin (M ~1.5–2d) → recipient-gate format-aware (M–L ~4–5d) → HKDF per-user crypto (M ~2–3d, gates Anthropic BYOK).
- **Track B (verification, BLOCKED on running host — needs user go):** chat F2 chart/map + F3 OCR live; workflow E2E reconcile vs workflow-qa-* backlogs; SearXNG self-host.
- **Track C (pending):** Matte Dark page rollout (9 pages, M–L, gated on visual sign-off).
- HELD per user: persist plan doc + reconcile INDEX (wait until dev team finishes push); draft Track A detailed TDD breakdown (await user go).
