# Checkpoint: detail (W1-B) — 2026-06-03

## What was done
- Plan: `docs/superpowers/plans/2026-06-03-v2-wave1-pkgB-detail.md` (writing-plans).
- TDD'd `ToolWaterfall` client component + pure `barWidthPct` helper. 8 tests pass.
- Wired into `/agents/[id]` page: replaced "Tool calls gần đây" list with `<ToolWaterfall/>`; added "Sub-agents" section from `s.subAgents`. Timeline + meta untouched.

## Files changed
- Created: `v2/src/components/agents/ToolWaterfall.tsx`, `.../ToolWaterfall.test.tsx`
- Modified: `v2/src/app/agents/[id]/page.tsx` (imports + 2 sections)

## Current state
- `npx vitest run src/components/agents/ToolWaterfall` → 8/8 pass.
- `npx tsc --noEmit` → clean (exit 0, no output).
- NOT committed (lead reviews). Did not run full `npm test` or `next build` (scope: own tests only).

## Decisions / deviations (surfaced to lead)
1. Bars are proportional-duration (width = durationMs/max), NOT absolute-timeline Gantt — locked prop `{name,durationMs,isError}` has no start/end. Matches lead's "relative to the max" spec.
2. Hardcoded Vietnamese strings (not `useT`): NO `I18nProvider` is mounted in `app/`, and the existing page hardcodes vi strings; `useT` would throw. Values mirror `session.*` dict (wfTitle, subs, subNoDesc, noToolCall). Swap to useT if a provider is added later.

## Next steps
- Lead: integration `npm test` + `next build`; review + commit.

## Blockers / Risks
- Could not verify via full `next build` (kept to own-test scope per instructions). Lead should run it at integration.
