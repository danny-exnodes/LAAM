# Checkpoint: wf-engine (G1 engine v2) — 2026-06-05

## What was done
G1: Workflow Engine v2 (condition + foreach + bounding + A1 follow-ups) on `feat/wf-engine`. All 6 plan tasks, TDD, one commit each.
- T1 types: WfConditionNode/WfForeachNode, Op/Comparator/Predicate, WfEdge.label, Budget+DEFAULT_BUDGET, StepRecord.parentNodeId.
- T2 predicate.ts: evalPredicate (eq/ne/gt/lt/gte/lte/contains/not_contains/exists/not_exists + all/any); arg-sink operands; exists/not_exists tolerate missing (catch resolve throw), other ops fail-loud.
- T3 validate.ts: assertRunnable (condition=exactly true+false edges; others <=1 out; <=1 in/no fan-in; 1 start; reachable/no-cycle DFS; foreach body recursive). A0 assertLinear/linearOrder kept (still tested).
- T4 engine.ts: recursive edge-walker; shared budget counter holder threaded through foreach recursion; foreach body fresh steps:{} + vars{item,index}; condition follows label===String(result); running StepRecord carries input preview; foreach children carry parentNodeId.
- T5 run.ts: persist run_step.input; map parentNodeId→foreach row id as parentStepId; try/catch around runWorkflow → finalize FAILED on engine throw (budget/validate/foreach-not-array); onStep DB writes fail-soft.
- T6: repointed POST /api/workflows assertLinear→assertRunnable.

## Files changed
- src/lib/workflow/types.ts, predicate.ts (new), validate.ts, engine.ts, run.ts (+ their .test.ts)
- src/app/api/workflows/route.ts, src/app/api/workflows/[id]/run/route.ts (buildRunNode narrowed agent|connector)

## Current state
- WORKING. `npx tsc --noEmit` = 0 (full repo). `npx vitest run src/lib/workflow` = 73 green (7 files). Full repo = 679 green (133 files), zero regressions.
- Commits: cf21e2a, 05579aa, 0965e66, 589d6fd, 63c1446, cc81e6f.
- A0 contracts intact: runNode(node,ctx)→Promise<unknown> + executors + resolveTemplate unchanged; engine handles condition/foreach internally. DB schema already had parentStepId+input (no migration).

## Next steps
- Backend only, no UI (per plan). PR / review when controller decides.

## Blockers / Risks
- None. Design concern (non-blocking): condition branches do not reconverge (tree, no fan-in) — DAG/merge deferred per plan. Budget is step/item count, not token-precise (deferred to preserve A0 runNode contract). Both are documented plan simplifications.
