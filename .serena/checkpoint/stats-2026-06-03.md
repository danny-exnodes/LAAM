# Checkpoint: stats — 2026-06-03

## What was done
- Wave 0 **Package B** (Task #2): ported v1 `lib/stats.js` aggregation into typed
  `computeStats(sessions): Stats` emitting the LOCKED `Stats` shape; added auth-guarded
  `GET /api/stats` (Drizzle select + project leftJoin, Date→ms).
- TDD: 19 logic tests + 2 route smoke tests = **21 passing**.
- Rule-13 guard test included: tool names "Bash" vs "bash" stay DISTINCT (no case-fold).

## Files changed (all NEW, in scope)
- `v2/src/lib/stats.types.ts` — SessionRow + locked Stats interface
- `v2/src/lib/stats.ts` — computeStats + buildActivity/durationMs helpers
- `v2/src/lib/stats.test.ts` — 19 logic tests
- `v2/src/app/api/stats/route.ts` — GET, auth() guard, Drizzle
- `v2/src/app/api/stats/route.test.ts` — 2 smoke tests (401 no-db; 200 payload)
- `docs/superpowers/plans/2026-06-03-wave0-pkgB-stats.md` — sub-plan
- (artifact) `v2/tsconfig.tsbuildinfo` — generated by my `tsc --noEmit`; untracked

## Current state
- `npx vitest run src/lib/stats src/app/api/stats` → 2 files, 21 passed.
- `npx tsc --noEmit -p tsconfig.json` → 0 errors (whole project).
- Uncommitted (left for lead review). No edits to package.json/vitest/tsconfig.

## Conflict resolved (AGENTS.md Rule 7)
Locked `Stats` interface differs from v1's return shape (records vs arrays-of-objects,
heatmap number[][] vs {cells,max}, activity {t,sessions,tokens}[] vs {bucketMs,points},
flat toolLeaderboard vs {mostUsed,mostErrors,slowest}). The LOCKED interface wins — it's the
contract Waves 1–4 consume. Ported v1 logic, emitted locked shape.

## Notes for consumers (Wave 2 Dashboard)
- `durationMs` is DERIVED in-fn = lastActivity - startedAt (>0); null/missing ignored in avg.
- `byProject.project` is the joined project.name (null → "(no project)"); fn keys on whatever
  string the route passes (Rule 13). Route does a leftJoin to projects.
- toolLeaderboard.avgDurationMs is a count-weighted average of per-session avgDurationMs.
- costUsd uses the stored column (no pricing-table fallback like v1).

## Next steps
- Lead: review + commit per Wave-0 coordination plan.
- Wave 2 will migrate Dashboard's inline aggregation to consume `/api/stats`.

## Blockers / Risks
- None. (Did not run full `npm test` or `next build` per scope; tsc + targeted vitest only.)
