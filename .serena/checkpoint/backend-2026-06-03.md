# Checkpoint: backend (W1-C) — 2026-06-03

## What was done
- Package W1-C (SSE enrich + bus publisher) — TDD, plan in `docs/superpowers/plans/2026-06-03-v2-wave1-pkgC-backend.md`.
- `api/events/route.ts`: factored row→LiveSession into exported pure `mapRowToLiveSession`; `snapshot()` now does explicit-column `select().from(agentSessions).leftJoin(projects, eq(projectId, id)).orderBy(desc(lastActivity))` → sets `projectName` (null when no project) + passes through `subAgents` jsonb (null default). Kept Date→epoch-ms. SSE framing/auth untouched.
- `api/sync/route.ts`: after a successful `syncLocalMonitoring()`, `publish({type:"sync"})` from `@/lib/events-bus`, then return result unchanged. No publish on 401 or on failure.

## Files changed
- Modified: `v2/src/app/api/events/route.ts`, `v2/src/app/api/sync/route.ts`
- Created: `v2/src/app/api/events/route.test.ts`, `v2/src/app/api/sync/route.test.ts`, plan md

## Current state
- `npx vitest run src/app/api/sync src/app/api/events` → 2 files, 5 tests pass.
- `npx tsc --noEmit` clean (whole v2). Mapper return type unifies with LOCKED LiveSession.
- NOT committed (lead reviews). Did not touch hooks/useLiveSessions.ts, components, package.json/vitest/tsconfig.

## Next steps
- Lead integration: full `npm test` + `npm run build`; manual live (click Đồng bộ → /agents refreshes via SSE).

## Blockers / Risks
- None. Note: sync test mock for events-bus must declare `publish` inside `vi.hoisted` (hoisting) — a top-level `const publish = vi.fn()` referenced in the factory throws "Cannot access before initialization".
