# Checkpoint: claude — 2026-06-10

## What was done
- Investigated whether the Local model (Ollama) is reachable from outside the host.
- Found Ollama bound to `127.0.0.1:11434` only (loopback) → not reachable externally.
  Verified empirically: localhost OPEN; LAN `192.168.1.122:11434` and Tailscale
  `100.104.39.38:11434` both TIMEOUT (firewall also filters).
- Per user choice ("expose via Tailscale"), exposed Ollama tailnet-ONLY via
  `tailscale serve --bg --tcp=11434 tcp://127.0.0.1:11434` (raw-TCP passthrough).
- Discovered Ollama anti-DNS-rebinding Host check: hostname → 403, only IP Host accepted.
  Tried `--tls-terminated-tcp` and `--https` reverse-proxy (both 403, proxy keeps hostname Host);
  settled on raw-TCP + IP as the only no-restart working mode.

## Files changed
- No repo source changed. Host runtime only: added Tailscale serve mapping (port 11434,
  tailnet-only). Existing 443-funnel→3900 and 8443→3100 serve entries left untouched.
- Wrote this checkpoint; appended `.serena/memories/decisions/poc-host-and-ollama-ops.md`.

## Current state
- WORKING: Ollama reachable from tailnet devices at `http://100.104.39.38:11434`
  (IPv6 `[fd7a:115c:a1e0::8533:2726]`). /api/version=0.30.7, /api/tags=qwen3-vl:8b-instruct-q8_0.
- Tailnet-only; LAN still TIMEOUT (good). Ollama still bound 127.0.0.1.
- Caveat: clients MUST use the IP, not the MagicDNS hostname (Ollama 403 on hostname).

## Next steps
- Optional (needs user OK + Ollama restart): reconfigure Ollama to accept the MagicDNS
  hostname so a clean `https://danny-gaming-pc.tail41dda4.ts.net:11434` works.
- Consider Tailscale ACLs to restrict which tailnet devices reach 11434 (Ollama has no auth).

## Blockers / Risks
- Ollama has NO auth: any tailnet device can use the GPU/model. Mitigate via Tailscale ACLs.
- FYI (separate from task): `:3900` is exposed to the public Internet via Tailscale Funnel — flagged to user.
- Revert exposure: `tailscale serve --tcp=11434 off`.
