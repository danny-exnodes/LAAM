# Checkpoint: claude (tech-lead) — 2026-06-06

## What was done
- Connectors **fix & improve, OAuth-first**. Branch `feat/connectors-oauth-expansion`, commit **1a721d7** (NOT pushed/merged).
- **P3 — OAuth Google in-app** (calendar/drive/gmail): `google-oauth.ts` (PKCE S256, exchange, refresh, id_token email); GET `/api/connectors/:id/authorize` + shared `/api/connectors/google/callback` (connectorId in encrypted state cookie); refresh pre-flight in `execute()`/`testConnector()` (one chokepoint for chat + workflow); tri-state status. No schema change (tokens in existing encrypted creds blob).
- **P4a — write-ready contract**: `ConnectorTool.kind` self-declared; `policy.ts` derives read/write from CONNECTORS registry (removed central `CONNECTOR_WRITES`/`READS` sets); fail-closed preserved.
- **P1 — UI polish** (gộp): Lucide icons (was first-letter), busy-state, disconnect error surfacing, 3-state badges, OAuth connect/reconnect anchors, `?connected=/?error=` toast.
- i18n +10 keys (vi/en/zh). `.env.example` + spec `docs/superpowers/specs/2026-06-06-connectors-oauth-google-design.md` with operator setup.

## Files changed
- Commit 1a721d7 — 26 files (962+/142-). New: `google-oauth.ts`(+test), spec doc.

## Current state
- **tsc clean; full suite 890/890 pass.** Core verified by tests, NOT by live Google round-trip.
- WorkflowEditor.test.tsx flaked once in a full parallel run, passed on rerun (no change to it) → flaky, not a regression.

## Next steps
- **Operator (user):** Google Cloud Console one-time setup (External+Testing, add test users, scopes calendar/drive/gmail .readonly, register redirect `<base>/api/connectors/google/callback`), set env `GOOGLE_OAUTH_CLIENT_ID/SECRET/OAUTH_PUBLIC_BASE_URL`, then live-verify.
- **P2** i18n connector-supplied content (name/blurb/help/setup still hardcoded vi) — needs design (dict-in-module vs central keys).
- **P4b** read-tools expansion (per-connector, parallel-safe now). Candidates: spec §4.
- **P5** write actions — only when user grants write perms; `gmail.send` needs OAuth write scope (re-consent). Write-gate (SP-2) already gates writes.

## Blockers / Risks
- Google Testing mode: refresh tokens die ~7 days → users reconnect weekly (by design; UI handles via needs_reconnect).
- ⚠️ Lesson: `git stash` mid multi-file work transiently reverted tracked edits (recovered via pop; confirmed intact). Avoid stash during active work.
- ⚠️ Lesson: a `cd` in a Bash compound command **persists** (Bash tool keeps cwd) → later `npx vitest` ran from wrong dir (setup-file misresolve, 12 false "fails"). Run from repo root / use absolute paths.

## Session 2 — P2 + P4b + P5 (merged OAuth, then continued to end of P5)
- **Merged** OAuth tranche to `main` (ff). Continued on branch `feat/connectors-p2-p5`, commit **798e160**.
- **P4b reads + P5 writes**: dispatched **6 parallel subagents** (one per connector, conflict-free via self-declared kind) → **+21 tools** (github/trello/jira/gcal/gdrive/gmail). Google connectors gained write scopes (calendar.events / drive.file / gmail.send) — need user re-consent.
- **Write surface now 11 tools**, all gated (confirm-card) + HIGH-blast fail-closed in workflows. Updated policy.test + registry.test audit lists.
- **P2**: connector blurb/help/setup → vi/en/zh (14 keys, UI `svc()` fallback helper).
- Verified: **tsc clean, full suite 922/922**.
- **TODO next session:** merge `feat/connectors-p2-p5` → main (if not done); operator Google Console setup + env for live; P5 write tools only fire after write-scope re-consent.

---

## Session 3 — World-Tools Layer (main session)

## What was done
- Tư vấn (Technical Consultant) → thiết kế → implement **World-Tools Layer** mở rộng harness: họ `web_*` (web_search SearXNG self-host $0, web_read promote fetch-url), `util_*` (util_calc), mở rộng `laam_*` (search_sessions, get_timeline, query_audit).
- Quy trình: brainstorming → spec → worktree cô lập → **TDD** (RED/GREEN từng tool) → wiring → verify đầy đủ.

## Files changed (branch `worktree-world-tools`)
- MỚI: `src/lib/web/{readable,searxng}.ts`(+test) · `src/lib/agent/tools/web/{read,search,index}.ts`(+test) · `src/lib/agent/tools/laam/{search-sessions,get-timeline,query-audit}.ts`(+test) · `src/lib/agent/tools/util/{calc,index}.ts`(+test) · `searxng/settings.yml`.
- SỬA: `src/app/api/fetch-url/route.ts` (dùng readable) · `src/lib/agent/registry.ts` (wire 3 họ) · `tools/laam/index.ts` · `docker-compose.yml` · `.env.example` · `CHANGELOG.md` · `README.md`.

## Current state
- ✅ **897 test xanh** (`npm test`), `tsc --noEmit` sạch (exit 0). **0 migration, 0 đổi hợp đồng SP-1**, không đụng connector/gate SP-2/schema.
- ⏳ CHƯA merge vào main. CHƯA E2E runtime (cần bật SearXNG + Ollama = bước host của user).

## Next steps
- Bật SearXNG (sửa `secret_key` trong `searxng/settings.yml`, `docker compose up -d searxng`, set `SEARXNG_URL`) → E2E chat "tìm web".
- Followups: [[world-tools-followups]] (eval scenarios, URL-citations, DDG fallback, promote OCR/geo).

## Blockers / Risks
- `web_search` cần SearXNG chạy — fail-soft nếu thiếu (không sập chat).
