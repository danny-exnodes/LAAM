# Checkpoint: connui — 2026-06-03

## What was done
- Package W4-U (Wave 4 Connectors): /connectors page + ConnectorsClient (v1 parity).
- TDD: wrote ConnectorsClient.test.tsx first (6 tests), then implementation. All 6 green.
- i18n: dictionaries/connectors.ts already had all 17 conn.* keys → NO new keys added.
- Added "/connectors" to app-header.tsx NAV (minimal, additive, between Graph and Machines).

## Files changed
- CREATE v2/src/app/connectors/page.tsx (thin auth shell + AppHeader current="/connectors")
- CREATE v2/src/components/connectors/ConnectorsClient.tsx (ConnectorsClient + inline ConnectorCard)
- CREATE v2/src/components/connectors/ConnectorsClient.test.tsx
- EDIT v2/src/components/app-header.tsx (one NAV entry)
- CREATE docs/superpowers/plans/2026-06-03-v2-wave4-pkgU-connui.md

## Current state
- `npx vitest run src/components/connectors` → 6/6 pass.
- tsc clean for MY files; remaining tsc errors are jira.ts (W4-C) and store.test.ts (W4-F) — other agents in progress, not mine.
- Behaviour matches v1 public/connectors.js: load list, cards (icon/name/blurb/badge/tool list),
  token fields (masked hint when set), connect→test, disconnect→reload, test inline ok/err, oauth disabled.

## Next steps
- TL integration: full npm test + build after F+C+A+T land.
- Icon note: v2 has no icon lib; rendered first-char glyph in accent badge (v1 lucide lookup unavailable, out of scope).

## Blockers / Risks
- None for U. Page is live once auth + /api/connectors route (connapi) are wired; framework table needs host migration.
