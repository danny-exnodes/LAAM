# Checkpoint: security-s1 — 2026-06-12

## What was done
- Phase S / Task S1 (RBAC enforcement). Closed the LIVE hole where RBAC was
  decorative (authorized() only checked logged-in → viewer/member could fire any
  mutation incl. real connector writes via workflows/[id]/run).
- New `src/lib/auth/rbac.ts`: `requireRole(session, roles[])` + `requireMutator(session)`
  (`{ok:true} | NextResponse`; 401 no session, 403 viewer/role-miss; fail-closed on
  missing role). `Role` from `roleEnum.enumValues`.
- Gated mutation routes (pattern: `const gate = requireMutator(session); if (gate instanceof Response) return gate;`):
  workflows POST, workflows/[id] PATCH+DELETE, workflows/[id]/run POST,
  connectors/[id]/[action] POST, conversations/[id] DELETE, access-tokens POST,
  chat POST (gated ONCE at entry, before isConfirmBody dispatch — covers message + confirm).
- Refactored 2 copy-paste owner/admin blocks in machines/route.ts + machines/[id]/route.ts onto `requireRole(['owner','admin'])` (Rule 7).
- TDD: helper unit test + per-route viewer-403 integration tests (assert no side effect).
  Updated pre-existing chat + workflows/[id] tests to give mocked sessions a role
  (they predate the gate; real sessions always carry a role).

## Files changed
- NEW: src/lib/auth/rbac.ts (+ rbac.test.ts)
- NEW tests: workflows/route.test.ts, workflows/[id]/run/route.test.ts, conversations/[id]/route.test.ts
- MOD routes: access-tokens, chat, connectors/[id]/[action], conversations/[id], machines, machines/[id], workflows, workflows/[id], workflows/[id]/run
- MOD tests: access-tokens, chat, connectors/[id]/[action], workflows/[id]
- Commit 34a9cea on feat/batch2.

## Current state
- WORKING. vitest scoped (src/lib/auth + src/app/api) 234 pass; full suite 1624 pass / 243 files; tsc --noEmit clean.

## Next steps
- S2 (SSE snapshot isolation) + S3 (off-boarding). Then Phase F.

## Blockers / Risks
- CONCERN (not in S1 scope): connectors GET OAuth flow (/api/connectors/google/authorize|callback)
  is NOT gated — a viewer could complete Google OAuth and connect live creds, bypassing the
  POST gate. Spec scoped S1 to the POST handler; gating GET needs a redirect-style 403 (not JSON).
  Flag for follow-up (S-phase or backlog).
- conversations/[id] PATCH (rename) left ungated (spec listed only DELETE); low risk since chat POST
  is gated so a viewer never creates a conversation to rename.
