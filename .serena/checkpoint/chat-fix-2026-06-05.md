# Checkpoint: chat-fix (truncation + sampler) — 2026-06-05

Vai trò: debugging (systematic-debugging) lỗi user báo: chat ở :8443 trả lời "đứt đoạn / cắt giữa chừng".

## What was done
- **Chẩn đoán (Iron Law: root cause trước):** đọc producer (route streamOllama) + consumer (ChatClient splitFrames) → loại giả thuyết frame/`␞`. User gửi export `chat.json` → **smoking gun: mọi reply bị cắt có `tokensIn + tokensOut === 4096` CHÍNH XÁC** ⇒ tràn context window (Ollama default `num_ctx=4096`, route chưa set).
- **Fix (2 lớp) — `4f83fb6`:** (1) set `num_ctx` mọi call Ollama qua env `CHAT_NUM_CTX` (default 16384); (2) `planHistory` bound replay theo KÍCH THƯỚC (char budget dẫn từ num_ctx, chừa output+system+tools) thay vì "giữ N lượt cuối" ⇒ 1 lượt tool khổng lồ không nuốt cả cửa sổ. +test.
- **Decision memo `c6dbd1d`** (`decisions/chat-context-window.md`).
- **Sampler (góp ý team Qwen3-Q8) — `b3db3a7`:** `presence_penalty` wired server-side (default 0.2, env `CHAT_PRESENCE_PENALTY`, body override) + default `temperature` 0.6 (DEFAULT_SETTINGS). +test. `.env.example` ghi rõ lộ trình 49152+q8KV + giải thích 250k bất khả thi (KV ~144KB/token).

## Files changed
- `src/app/api/chat/route.ts` (num_ctx + REPLAY_BUDGET_CHARS + presence_penalty) · `src/lib/agent/summarize.ts` (planHistory size-budget) · `src/components/chat/types.ts` (temperature 0.6) · `route.test.ts` + `summarize.test.ts` · `.env.example`.

## Current state
- Committed (`4f83fb6` · `c6dbd1d` · `b3db3a7`). **tsc sạch + 499 test main pass.** Fix LIVE qua HMR dev server (:3100→:8443): num_ctx 16384, temp 0.6, presence_penalty 0.2.
- Phân tích VRAM (RTX 5070 Ti 16GB, model 9.16GB Q8, KV 144KB/tok FP16): max FP16 ~32k; q8 KV ~64–76k; **250k bất khả thi** (~37GB KV).

## Next steps (USER)
- **Để lên 49152 ctx (THỨ TỰ bắt buộc):** (1) `setx OLLAMA_FLASH_ATTENTION 1` + `setx OLLAMA_KV_CACHE_TYPE q8_0` → restart Ollama; (2) `CHAT_NUM_CTX=49152` vào `.env` → restart dev. (Nếu không bật q8 KV trước → 49152 OOM ~17.5GB.)
- Verify live: gửi lại trong hội thoại dài → trả lời đủ. (Có thể nhờ tôi check `nvidia-smi` sau khi bật q8 KV.)
- Follow-up: presence_penalty UI slider (SettingsPanel + ChatClient send) — defer vì session FE/chat-ux đang active trong tree.

## Blockers / Risks
- 49152 cần q8 KV TRƯỚC (else OOM). Nhiều session song song đang sửa cây chung (FE/chat-ux) — chỉ commit file của mình.
