# Checkpoint: list (agent) — 2026-06-03

## What was done
- Wave 1 **Package W1-A** (Agents list: client + filters + live + CSV), TDD.
- Plan: `docs/superpowers/plans/2026-06-03-v2-wave1-pkgA-list.md`.
- Pure `applyFilters` (q/project/model/status incl. `stuck` via isStuck/branch/window) + `AGENT_CSV_COLUMNS` + `toCsvRow` mapper.
- Components: `AgentsClient` (useLiveSessions → derive options → filter → group by projectName, null→"Khác" → render), `FilterBar`, `AgentCard` (status/stuck/LOCAL badges + per-card 1s live ticker via local useEffect interval + SubAgentList), `SubAgentList`.
- Rewrote `app/agents/page.tsx` → thin server shell (auth guard + AppHeader + AgentsClient).

## Files changed
- Create: `v2/src/components/agents/{filters.ts,SubAgentList.tsx,AgentCard.tsx,FilterBar.tsx,AgentsClient.tsx}` + matching tests (`filters.test.ts`, `*.test.tsx`).
- Rewrite: `v2/src/app/agents/page.tsx`.
- Plan doc.

## Current state
- `npx vitest run src/components/agents src/app/agents` → **6 files, 32 tests, all pass**.
- `npx tsc --noEmit` → exit 0 (clean).
- Did NOT git add/commit (left for TL review).

## Deviations / notes (flagged to TL)
- `FilterBar` gained an **additive** `onExport: () => void` prop (CSV button lives in the bar but needs the filtered rows held by AgentsClient). Locked prop shape otherwise unchanged.
- `AGENT_CSV_COLUMNS` omits v1's projectPath / user|assistantMessageCount (not present on LiveSession) — documented in code comment, fail-loud rather than blank columns.
- Root `app/layout.tsx` was wired to mount `I18nProvider` globally (by TL/another agent mid-session) → the page shell does NOT double-wrap the provider. (layout.tsx is NOT an owned file; left as-is.)
- Hit the warned jsdom phantom failure once → cleared `node_modules/.vite` + `.vitest`, passed.

## Next steps
- TL review + commit. Integration: `npm test` (all waves) + `npm run build` + manual live `/agents`.

## Blockers / Risks
- None. AgentsClient relies on the W1-C enriched SSE snapshot (projectName + subAgents) being live — that package is marked completed.
