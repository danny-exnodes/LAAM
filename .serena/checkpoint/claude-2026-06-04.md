# Checkpoint: claude — 2026-06-04

## What was done
- **Tech-lead review hosting v2** trên máy host (RTX 5070 Ti **16GB** VRAM — WMI báo nhầm 4GB; Ultra 9 285K 24c; 128GB RAM). Yêu cầu môi trường 2 tầng: **lõi** (Node≥20 ✅24.11 + Docker ✅ → Postgres) chạy được ngay 0 cài thêm; **AI** (Ollama + Tesseract) cho chat/OCR. Probe: node/docker/git/winget có sẵn; Ollama+Tesseract thiếu; ports 3000/5432/11434 trống.
- **Tra Gemma 4** (ra 2026-04-02, sau cutoff): E2B/E4B/12B/26B-A4B/31B. Fit 16GB: E2B/E4B/12B ✅, 31B ❌.
- **Chốt model POC** (POC cần chat+OCR+connector+tool-call): **1 model `qwen3-vl:8b-instruct-q8_0`** (Q8 9.8GB). Lý do: smart-routing **chưa hề implement** (grep rỗng; tool-loop dùng `payload.model`); Qwen3-VL có badge vision+tools+thinking, fit 16GB; Q8 tăng độ chính xác JSON tool-call. Gemma4-12B trên Ollama chủ yếu `12b-mlx` (Apple) → để sau.
- **Phát hiện code:** ảnh → Tesseract OCR → text (vision model CHƯA nối). `DEFAULT_CHAT_MODEL` env là ĐỦ để đổi model (xác minh qua `/api/chat/info` + `ChatClient.tsx:52-56`).
- Soạn `v2/setup-poc.ps1`.

## Files changed
- Mới: `.serena/memories/decisions/poc-model-choice.md`, `v2/setup-poc.ps1`, checkpoint này.
- Sửa: `.serena/memories/INDEX.md` (decision + status POC).
- **Không sửa source v2.**

## Current state
- Quyết định host + model POC đã khoá. Code v2 nguyên trạng. **Chưa chạy setup, chưa nghiệm thu live.**

## Next steps
- Chạy `v2/setup-poc.ps1` (Admin) → `cd v2; npm run start` → nghiệm thu 4 nhiệm vụ (chat vi / OCR ảnh Việt / connect GitHub / "liệt kê repo" → tool-call).
- Nếu pass tool-call → chốt single-model. Vision ảnh thật = enhancement sau (đẩy `images` vào payload Ollama; có thể thay Tesseract bằng OCR của qwen3-vl).

## Blockers / Risks
- Tag `qwen3-vl:8b-instruct-q8_0` phải pull được (cần Ollama mới). Tesseract vie/chi_sim tải traineddata từ GitHub. Docker Desktop phải đang chạy. winget/PATH cần Admin.
- Q8 để lại ~6GB đệm — nếu nối vision/parallel nặng thì hạ về `qwen3-vl:8b` (Q4, 6.1GB), đổi 1 dòng `.env`.
