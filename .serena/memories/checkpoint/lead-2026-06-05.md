# Checkpoint: lead (Technical Leader) — 2026-06-05

Nhận báo cáo QA E2E Chat (qa-e2e-chat-2026-06-05) → fix + improve **toàn bộ** 12 mục.

## What was done (tất cả ✅, có test)
- **U1** composer `relative` · **F1** slash commands wired · **F4** `titleHint` (title không lẫn byte) · **U2** model name động (bỏ "Gemma") · **U3** i18n header (theme/sync/account) · **U-minor** sidebar skeleton.
- **F2** rich-render: system prompt dạy ```chart/```map + **client-side geo-resolve** (geocode/route/nearby từ TÊN địa điểm — Rule 13, không bịa polyline). `src/lib/chat/geo-resolve.ts`.
- **F3+FEAT-4** `GET /api/ocr {available}` + composer degrade mềm (báo trước, bỏ qua call khi thiếu tesseract).
- **UX-1** auto-send · **UX-2** URL inline (bỏ window.prompt) · **UX-4** scroll-btn ngưỡng 80 · **UX-6** recent convs · **UX-7** a11y focus-within.
- **FEAT-1** nhóm thời gian + bulk-delete + pin(localStorage) + content-search (`/api/conversations?q=`).
- **FEAT-2** proactive → frame `proactive` + `ProactiveCard` (dismiss); ngưỡng env `PROACTIVE_STUCK_MIN/COST_USD`.
- **FEAT-3** export PDF + copy-all + tổng token. **FEAT-5** `demo_create_task` (write-gate demo không cần credential) + `docs/demo-connector-write-gate.md`.

## Files changed (chính)
- chat FE: `components/chat/{ChatClient,Composer,ChatExport,ConversationSidebar,MessageItem,SettingsPanel,types}.tsx` + mới `{ProactiveCard,convGroups,pins}.ts(x)`.
- render/lib: `components/render/MapBlock.tsx`, `lib/chat/{geo-resolve,frames}.ts`, `lib/agent/context.ts`.
- backend: `app/api/chat/route.ts`, `app/api/conversations/route.ts`, `app/api/ocr/route.ts`.
- safety/connector (FEAT-5): `lib/connectors/demo.ts`, `lib/agent/safety/{policy,preview}.ts`.
- i18n: `dictionaries/{chat,common}.ts`. Docs: `CHANGELOG.md`, `.env.example`, `docs/demo-connector-write-gate.md`.

## Current state
- ✅ **540 test xanh** (từ 499), `tsc --noEmit` exit 0. **Không đổi schema** (pin=localStorage; không migration).
- CHƯA commit (chờ user review). KHÔNG tự chạy dev/ollama/preview (theo agent-ops-rules).

## Next steps (cần RUNTIME verify — không làm được trong code)
- Live model qwen3-q8 **thực sự emit** ```chart/```map? (prompt đã dạy; cần test trên Ollama).
- OCR: chạy bản **Docker** (đã bake tesseract) hoặc cài native trên host trần.
- Demo write-gate: bật connector Demo ở /connectors rồi test Confirm Card (xem doc).

## Blockers / Risks
- F2 phụ thuộc model tuân thủ format + dịch vụ OSM (Nominatim/OSRM/Overpass) online.
- UX-5 (chip "đang gọi tool" realtime): tool-loop non-stream → chỉ có "đang soạn…" + trace sau; realtime cần SP-4 "trực-tiếp" (deferred).

## Round 2 — 9 góp ý nâng cấp (sau E2E live, 2026-06-05)
Sau khi build lại prod + E2E live (chart/map/geo/slash/i18n/proactive/export đều OK), làm tiếp 9 góp ý:
- S1 backfill title (`lib/chat/title.ts` retitleFromMessage) + endpoint `POST /api/conversations {backfill-titles}` + nút "Dọn tiêu đề" + badge "trùng". S9 `{retitle,id}` + ✨ per-conv.
- S2 proactive dismiss bền (localStorage TTL 24h, `proactiveDismiss.ts`) + frame thêm `key`/`sessionId` → click mở `/agents/[id]`.
- S3 **realtime ĐÃ LÀM**: refactor `/api/chat` POST → `streamMainTurn` (1 stream): tool frames phát LIVE qua onEvent→enqueue; suspend(pending_write)+persist inline; bỏ `suspendForConfirm`; `streamOllama`/`streamText` giữ cho confirm. FE: MessageItem hiện ToolTrace lúc empty+streaming → chip "…". tsc sạch, 1141 test xanh. ⚠️ POST không có test tích hợp → verify bằng E2E live.
- S4 chart value-labels + cao 300 + bo góc. S5 `looseJsonParse` (lib/chat) + map "Thử lại" + chart hiện raw khi lỗi.
- S6 prompt near vs geolocation + retry locationDenied. S7 tổng token ở header. S8 aria-label lang i18n (native select sẵn a11y).
- Verify: tsc sạch, test xanh (suite nay gồm cả eval-project của session khác — chạy 2x, 0 fail).
- **Đã commit + merge main**: `96fb1c8` (S1/S2/S4-S9) · `97a50fd` (S3 realtime) · `4f35ba5` (fix backfill conv rỗng tin nhắn). Rebuild image + redeploy (SHA match, healthy).
- **E2E live (Chrome) ĐỢT 2 — PASS hết**: S3 tool-loop gọi `laam_list_agents` → trace "Đã dùng 1 công cụ" + "Nguồn" (pipeline streamMainTurn chạy, không vỡ); S7 "446 token · miễn phí" ở header; S2 alert link `/agents/[id]` + dismiss bền (5 key vào localStorage); S4 nhãn 12/19/9/15 trên cột; S1 dọn title (retitled 2, **stillPolluted 0**, nút eraser tự ẩn). Console sạch.
- **Bug E2E phát hiện+sửa**: backfill bỏ qua conv 0 tin nhắn (đã xoá) → fallback dọn từ chính title (`4f35ba5`), verified live.
- **Đã push `origin/main`** (`1ce78cb..3865b7b`, 5 commit). Toàn bộ chat QA + 9 nâng cấp + S3 realtime: code + test + E2E live + lên remote. DONE.
- **Hotfix `3793ec0`**: chart NaN — `?? 0` không bắt `Number("x")=NaN`; nhãn S4 phơi ra → React warn "Received NaN". Thêm `finiteNum()` (bar/line+pie). Lưu ý: warning là **dev-only** (Turbopack); prod (next start) vốn ẩn. Đã push origin/main.
