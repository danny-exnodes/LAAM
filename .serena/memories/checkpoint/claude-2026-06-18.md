# Checkpoint: claude — 2026-06-18

## What was done
- Added **BytePlus ModelArk** as a 3rd chat provider (alongside Claude + Local Qwen), scoped to user's choices: **full-agent (tools)** + **single org env key**.
- New adapter `src/lib/llm/byteplus.ts` (plain fetch, NO new dep): `BYTEPLUS_MODELS` whitelist, `isBytePlusModel`, `byteplusChat` (non-stream round → OllamaChatResponse, OpenAI args JSON-string→object), `byteplusStream` (SSE → {delta,usage}), `BytePlusUnavailableError`, `toOpenAI` convo translation (synth tool_call ids, pair tool_call_id, orphan web_read-nudge → user).
- Wired `route.ts`: BytePlus branch mirrors Ollama tool-loop but transport=byteplus + Claude-style stream-error handling; `handleConfirm` BytePlus branch + `streamByteplusCompletion`. Extracted shared `emitPendingWriteSuspend` (write-gate suspend) used by both Ollama + BytePlus.
- `/api/chat/info` exposes env-gated `byteplusModels`; `SettingsPanel` 3rd optgroup; `ChatClient` state+prop; i18n `chat.grpByteplus` (vi/en/zh); `.env.example` block; CHANGELOG + README.

## Files changed
- NEW: `src/lib/llm/byteplus.ts`, `src/lib/llm/byteplus.test.ts`
- MOD: `src/app/api/chat/route.ts` (+292/-…), `route.test.ts`, `info/route.ts`, `info/route.test.ts`, `components/chat/SettingsPanel.tsx`, `ChatClient.tsx`, `i18n/dictionaries/chat.ts`, `.env.example`, `CHANGELOG.md`, `README.md`
- Branch: `worktree-byteplus-provider` (worktree under `.claude/worktrees/`). NOT merged, NOT committed.

## Current state
- **2070 tests pass**, tsc clean (exit 0). +35 new tests (adapter 29, route 2, info 2). No Ollama/Claude regression (suspend extraction + new branch verified).
- **Adversarial review done** (4-lens workflow + verification). 2 real bugs FIXED test-first: (a) `byteplusStream` fabricated 0/0 usage → fake billing (now omits usage when absent); (b) empty `choices[]` silent zombie round → now throws fail-loud. 3 "confirmed" findings REJECTED with code-traced reasoning (SSE-unescaped-newline = impossible for compliant SSE + matches Ollama; F1-guard-pre-delta = full="" so nothing to guard, matches Claude; partial-text-no-marker = platform-wide, matches Claude/Ollama, diverging would break Rule 7/11). 4 verifier-killed false positives (incl. "reject quick-tools for BytePlus" — wrong, BytePlus IS a tool provider).
- Working: BytePlus model selection → full tool-loop + streaming + write-gate + confirm-resume; env-gated picker optgroup; fail-loud coded notices; summarize/proactive stay local.

## Run-until-done tool-loop redesign (2nd task, same session)
- **Problem:** agent stopped after 3 tools on "summarize 10 emails then send" — `runToolRounds` capped `maxRounds=4` + force-text last round. User rejected "bump to 8" band-aid → wanted complete solution.
- **Built (designed via workflow, TDD):** (1) **run-until-done** — natural completion is primary exit; backstop now 25 (env `CHAT_MAX_ROUNDS`, ceiling 50), only forces text on the final round. (2) **`src/lib/agent/loop-context.ts`** (new, pure) `evictOldToolResults` — clears oldest tool-result bytes (keep recent 3, pin user/assistant) when convo exceeds `budgetChars`; provider-aware (local=REPLAY_BUDGET_CHARS, BytePlus=BYTEPLUS_TOOL_BUDGET_CHARS ~400K). (3) **repeat-guard** (tool+args, threshold 3). (4) **honest backstop notice** (onBackstop → tri-lingual, Rule 12). Write-gate unchanged.
- **Files:** `orchestrator.ts` (signature: 4th arg `maxRounds:number` → `opts:{maxRounds,budgetChars,keepRecent,onBackstop}`; return STILL `ChatMessage[]` — kept array to avoid breaking 4 consumers), `loop-context.ts` (new), `route.ts` both branches (opts + notice), `scripts/eval/runner.ts` (opts), `.env.example`, orchestrator.test.ts (rewrote "bounded" → 5 tests).
- **Verified:** 2080 tests, tsc clean. Worktree dev on :3101 restarted with new code (was bvth12si8→killed, now bve0gsi2p).
- **Deferred:** live eval re-baseline (default 4→25 changes scorecard — needs `npm run eval` on Ollama); optional wall-clock `LOOP_MAX_MS` budget; workflow agent-node onBackstop notice.
- **NOT live-verified yet:** "get 10 emails" on gpt-oss + local Qwen (user to re-test on :3101).

## Next steps
- Verify LIVE against a real BytePlus account: confirm exact model ids accepted (date-stamped vs `ep-xxxx`), `stream_options.include_usage` usage shape, tool-call wire format. (All external-confirm items flagged in [[byteplus-modelark-facts]].)
- Follow-ups (deferred): BytePlus vision (OpenAI image_url encoding — buildOllamaPayload images currently dropped for BytePlus); per-user BYOK key (connector, not core); per-conversation cost display (BYTEPLUS_PRICING like Claude); consider unifying Ollama+BytePlus tool-path (structural duplication accepted now).

## Blockers / Risks
- **No live verification** — all BytePlus API facts are doc-derived (post-cutoff, adversarially verified but not run against the API). The `model` id format + usage chunk shape are the highest-risk unknowns.
- Structural duplication of the tool-loop scaffolding between Ollama and BytePlus branches (~60 lines) — security-critical suspend IS shared, but write-guard wiring is duplicated (low drift risk; finalizeTurn owns the actual guard).
