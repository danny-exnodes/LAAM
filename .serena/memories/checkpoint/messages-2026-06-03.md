# Checkpoint: messages (W3-C) — 2026-06-03

## What was done
- Wave 3 Package W3-C — chat message render + actions + export (presentational components), TDD.
- Plan: `docs/superpowers/plans/2026-06-03-v2-wave3-pkgC-messages.md`.
- 3 components built to the LOCKED prop contracts (types from `@/components/chat/types`):
  - `MessageItem` — assistant content → Wave 0 `MarkdownView`; user → plain pre-wrap bubble. Hover action toolbar: Copy (both roles), Edit (user only), Regenerate (assistant only), Delete (both); edit/regen/delete disabled while streaming, copy always enabled (mirrors v1 chat-actions.js). Empty assistant + streaming → `chat.thinking` placeholder. Relative timestamp from `createdAt` (ms) via a tiny local `relTime()`.
  - `MessageList` — maps messages → MessageItem, threads the 4 callbacks, trailing `<div data-scroll-anchor/>`.
  - `ChatExport({messages,title})` — MD button → `downloadMarkdown(`${title}.md`, toMarkdown(messages))`; JSON → `downloadJson(`${title}.json`, messages)` (Wave 0 export).
- Labels via `useT(dictionaries/chat)`. **No dict keys added** — every needed key already existed in `chat.ts`.

## Files changed
- Created: `v2/src/components/chat/MessageItem.tsx` + `MessageItem.test.tsx`
- Created: `v2/src/components/chat/MessageList.tsx` + `MessageList.test.tsx`
- Created: `v2/src/components/chat/ChatExport.tsx` + `ChatExport.test.tsx`
- Created: plan file above. (No edits to types.ts / package.json / i18n / sibling-owned files.)

## Current state
- `cd v2 && npx vitest run src/components/chat/MessageList src/components/chat/MessageItem src/components/chat/ChatExport` → **3 files, 15 tests pass**.
- `tsc --noEmit` clean for all three components + their tests.
- Left UNCOMMITTED for TL review (per instructions).

## Next steps
- TL integration: ChatClient wires MessageList (passing copy/edit/regenerate/delete handlers + streaming) and ChatExport (messages + a sane title). Composer/SettingsPanel/ConversationSidebar from the composer agent.

## Blockers / Risks
- None. Deviation: `relTime()` is local (not `format.ago`, which takes a Date and is vi-only) — kept dependency-free + testable; renders the same Vietnamese phrasing. Flag if TL wants i18n-aware relative time.
- NB: agent threads reset cwd to repo root between bash calls — vitest MUST be run with `cd /Users/lion/Projects/LAAM/v2 &&` prefix or the `@` alias fails to resolve (looks like a phantom "no tests" failure otherwise).
