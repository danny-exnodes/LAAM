# Checkpoint: QA E2E Chat — 2026-06-05

Vai trò: **QA/QC Leader**. Test E2E feature **Chat** trên **PRODUCTION** (`https://danny-gaming-pc.tail41dda4.ts.net`, :443) qua **Claude in Chrome** (Browser local Windows, đã đăng nhập sẵn). Model `qwen3-vl:8b-instruct-q8_0` (Ollama GPU, rất nhanh).

## What was done
- **Fix `.claude/launch.json`** (preview `spawn sh ENOENT` / `spawn npm run dev ENOENT` trên Windows) → `runtimeExecutable:"node"` + `["node_modules/next/dist/bin/next","dev","-p","3100"]`. `preview_start` OK (reused server :3100).
- Test ~18 nhóm qua UI thật: load/auth, empty-state + sample prompts, settings, send/stream (ngắn+dài), token/timestamp, message actions (edit verified), slash menu, conversations (create/persist-reload/search/+Mới), export dropdown, responsive (drawer + bottom nav), dark/light, i18n vi/en, auto-scroll, console.

## Findings chính
- **F1 (cao)**: slash `/moi /xoa /xuat /caidat` inert — chỉ clear input, không thực thi (chỉ `/dung` chạy). `Composer.tsx:61-66`. **Verified live** (/moi không tạo conv mới; "+ Mới" thì có).
- **F2 (cao)**: rich-render chart/map KHÔNG kích hoạt — chart→ASCII, directions→text; **0 call** `/api/geocode|route|nearby`. Model không emit ```chart/```map, không gọi geo-tool.
- **F3 (cao)**: OCR chết trên prod (`"OCR chưa sẵn sàng: thiếu tesseract"`) → ảnh attach fail.
- **F4 (tb)**: title hội thoại lẫn raw bytes attachment (PDF `%PDF-1.3…` thành title).
- **U1 (cao, ĐO ĐƯỢC)**: composer floating lệch **144px** trái cột message (center 705 vs 849) + tràn full-viewport dưới sidebar; `<section>` thiếu `relative` (`ChatClient.tsx:454`). Fix = 1 class.
- **U2 (tb)**: "Gemma" hardcode (empty-state + placeholder, cả 3 dict vi/en/zh) trong khi model = qwen3.
- **U3 (tb)**: nút header (theme/sync/account) không i18n — giữ tiếng Việt ở EN.

## Working OK
streaming, token count (201 vào/193 ra), timestamp, persistence reload, search, export MD/JSON + click-outside, responsive drawer + bottom-nav, i18n body vi/en, dark/light, console **sạch hoàn toàn**, SP-3 proactive (stuck+cost, có điều kiện — không spam mọi tin).

## Chưa verify (limitations)
abort thật (model quá nhanh, xong trước khi bấm Dừng), URL attach (`window.prompt` native), connector confirm-card (chưa cấu hình connector), tool trace/citations (không có tool call để hiện), zh i18n.

## State
App đã khôi phục VI + theme system. `.claude/launch.json` đã sửa (CHƯA commit). Báo cáo 4 nhóm đã gửi user. **Lưu ý cập nhật**: dev server ở **:3100** (không phải :3000); prod ở tailnet :443; funnel :8443 đã tắt (502).
