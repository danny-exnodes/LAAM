# Checkpoint: i18n (Package A) — 2026-06-03

## What was done
- Implemented Wave 0 Package A — v2 i18n (vi/en/zh) per the LOCKED interfaces.
- Pure resolver `resolve(dict, lang, key, vars?)` with active-lang → vi → key fallback + `{var}` interpolation (mirrors v1 i18n.js).
- React `I18nProvider` / `useLang()` / `useT(namespace)`; `setLang` persists to `laam_lang` cookie.
- `cookie.ts`: `readLangFromCookie` (server+client) / `writeLangCookie` (client) + `LANG_COOKIE`.
- Ported all 5 dictionaries from v1 with EXACT key parity vi/en/zh (verified via a throwaway sandbox checker against the v1 sources, since removed):
  - common (from i18n.js shared block) = 34 keys
  - dashboard (i18n.dash.js) = 106 keys
  - agents (i18n.agents.js, agents.* + session.*) = 68 keys
  - chat (i18n.chat.js) = 157 keys
  - connectors (i18n.connectors.js) = 17 keys
- Dict shape transposed from v1 language-first nesting → flat key-first `{ 'dotted.key': {vi,en,zh} }` to match the locked `Dict = Record<string,{vi,en,zh}>`.

## Files created (only Package A scope)
- src/i18n/{types,index,cookie}.ts (+ .test.ts)
- src/i18n/provider.tsx (+ .test.tsx)
- src/i18n/dictionaries/{common,dashboard,agents,chat,connectors}.ts (+ .test.ts)
- docs/superpowers/plans/2026-06-03-v2-wave0-packageA-i18n.md (sub-plan)
- (removed throwaway v2/scripts/i18n-parity-check.cjs after verification)

## Current state
- `npx vitest run src/i18n` → 9 files, 23 tests PASS.
- `npx tsc --noEmit` → 0 errors project-wide.
- Did NOT touch package.json / vitest.config.ts / vitest.setup.ts / tsconfig.json. Did NOT git add/commit. Files left uncommitted for team-lead review.

## Notes / for team-lead
- My sub-plan's hand-estimated key counts (35/110/61/156) were wrong; the ports match the v1 source exactly (34/106/68/157). Tests assert the true counts.
- Naming overlap (harmless): `conn.*` exists in BOTH `common` (connection status: connecting/live/lost) and `connectors` (page strings). No collision because each is consumed via its own `useT(namespace)`. Wave 4 must use the `connectors` dict for the page.
- HTML kept verbatim in `dash.stuck.banner` / `dash.cost.total` (trusted dictionary text) — sanitize at render if injected as HTML.
- Hit a stale vite/vitest cache (likely from parallel teammate runs) that dropped the jsdom env → RTL tests failed with "document is not defined". Fixed by `rm -rf node_modules/.vite node_modules/.vitest`. Flag if other teammates see the same.

## Next steps
- Team-lead reviews + commits Package A.
- Waves 1-4 consume these dicts via `useT(<namespace>)`.

## Blockers / Risks
- None.
