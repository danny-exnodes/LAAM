# Checkpoint: cto — 2026-06-06

## What was done
- User (đóng vai CTO) giao: review tin nhắn consultant `claude-wdk-research` về roadmap mở rộng Workflow (durable-execution, học từ Vercel WDK) → nghiên cứu, đánh giá, phản hồi trên Serena.
- Boot Protocol: INDEX → comms `consultant-to-cto-wdk-comparison` → decision `workflow-wdk-comparison-learnings` → checkpoint `claude-wdk-research` → báo cáo `docs/workflow-sdk-vercel-comparison-2026-06-06.md` → decision `workflow-orchestration-architecture`.
- **Verify-not-prose (consultant yêu cầu):** đọc CODE để verify 4 claim load-bearing — `engine.ts:123` (ctx full-fidelity RAM) · `run.ts:17-25,83,116` (cap 256KB chỉ persist, PIN-D4b) · `interpolate.ts:34-35` (arg-throw/text-"" khi missing) · `schedule.ts:46,126` (tickClaim atomic vs tickExecute KHÔNG atomic) · `resume.ts:56-58` + `audit.ts:6-7,43-49` (nonce backed audit_log: window-10'/no-unique/key-ngẫu-nhiên) · `schema.ts:331-373` (status text-tự-do, run_step đã có `skipped`).
- Đối chiếu P5 (editor UX) — orthogonal, parallel-safe (grep durable/resume = 0 match).

## Files changed (KHÔNG đụng code app, KHÔNG chạy service)
- `comms/active/consultant-to-cto-wdk-comparison.md` → **append CTO VERDICT** + flip header RESOLVED → **moved `comms/resolved/`**.
- `decisions/workflow-wdk-comparison-learnings.md` (+section "CTO Verdict 06-06").
- `INDEX.md` (cập nhật pointer line WDK: ✅ CTO ký + tóm tắt verdict).
- `.serena/checkpoint/cto-2026-06-06.md` (file này).

## Current state — verdict đã ký, consultant CLEAR cho P0
- **borrow-not-adopt ✅** · **framing ✅ sharpen** ("3 trigger→2 suspend-mode→1 resume-spine"; Q2≡R1 2 phía).
- **R1 ✅ hybrid:** planned=exact qua cột mới `suspendedContext` (no-cap); crash=best-effort + **RÀNG BUỘC truncation-guard** (detect `_truncated`→ read re-run / write FAIL-LOUD, KHÔNG `""` âm thầm).
- **R4 ⚠️ BÁC reuse `audit_log`** → bảng riêng `workflow_node_idempotency` UNIQUE`(runId,nodeId,iterIndex)` + atomic ON CONFLICT.
- **R3 sharpen:** wake atomic (`UPDATE WHERE status='sleeping' RETURNING`), PIN-D1 mở target.
- **Sequencing:** P0a spine (no-UI) → P0b sleep; retry+hooks = P1.

## Next steps (consultant)
- `superpowers:writing-plans` P0a→P0b (worktree, đụng engine), success-criteria = §3 guard + §4 bảng riêng + §R3 atomic + 3 test (crash no-double-send / sleep-3d idempotent ngoài window-10' / truncate read-rerun-vs-write-failloud).
- Gửi plan vào `comms/active/` cho CTO gate **trước** `executing-plans`.

## Blockers / Risks
- Không. Thuần review/doc. Quyết-định-cuối open-Q (durable = điều kiện cần cho scheduled-production?) thuộc user — CTO đã khuyến nghị (chặn nhãn "tin cậy unattended", KHÔNG chặn ra mắt; manual best-effort ship ngay).

---

# Session 2 (cùng ngày) — CTO review **Eval v2**

## What was done
- User giao "review plans upgrade Eval". Đọc `comms/active/consultant-to-cto-eval-v2.md` + proposal `specs/2026-06-06-eval-v2-proposal.md` + decision `harness-reliability-eval` + backlog `harness-eval-next-phase`.
- **Verify-code:** `scripts/eval/union-tools.ts:8` (eval nhồi live `INTERNAL_TOOLS`) · `suite.eval.ts:21` · `registry.ts:13` (LAAM+WEB+UTIL) · `types.ts:25` (`extraToolSchemas` hook đã có) · `vitest.eval.config.ts` (host-only) · `trace.ts:21` (`deriveCitations` trả TÊN, không URL).
- **2 phát hiện đắt:** (1) E0 chỉ đo bloat nội-bộ 5→11, **KHÔNG đo ~40 connector** (union không gồm connector) → kéo selection-at-scale lên ngang E1; (2) Q3 over-couple — grader `citesRealUrl` self-contained, KHÔNG cần W-D6.

## Files changed
- `comms/active/consultant-to-cto-eval-v2.md` → **append CTO VERDICT** + flip RESOLVED → **moved `comms/resolved/`**.
- `decisions/harness-reliability-eval.md` (+section "Eval v2 — CTO Verdict").
- `INDEX.md` (cập nhật pointer eval).
- Checkpoint này (append session 2).

## Current state — verdict ký, consultant CLEAR
- Q1 re-slice: **E0 (user chạy) → E1 + selection-at-scale → E2-write sau**. Q2 đo **đường-cong** sel-vs-#tool, distractor=connector thật, ngưỡng drop-tương-đối **advisory** → fork tool-subsetting/routing/D-RUNTIME. Q3 grader CÓ/W-D6 KHÔNG. Q4 measure-only. Q5 hoãn CI-gate (variance + host-only infra).

## Next steps
- **Nút thắt = E0 cần USER chạy `npm run eval`** (agent KHÔNG chạy — `agent-ops`). Có scorecard → CTO đọc diff 11-tool vs baseline 5-tool.
- Consultant `writing-plans` E1+at-scale (worktree) → gate `comms/active/`.

## ⚠️ Pending khác (ngoài task Eval) — cần CTO xử lý phiên sau
- **`comms/active/consultant-to-cto-workflow-p0-plans.md` (🔴 OPEN)** — consultant đã viết plan P0a (resume-spine, 8 task) + P0b (durable-sleep, 11 task) từ verdict workflow session 1, xin CTO gate (7 quyết định tự chốt D1–D7). **Chưa review** (user chỉ giao Eval). Surface cho user.

---

# Session 3 (cùng ngày) — CTO GATE workflow P0 plans

## What was done
- User OK → gate cả 2 plan (P0a resume-spine 8 task + P0b durable-sleep 11 task).
- **Verify-code (trace trình tự, không tin bảng "honored"):** `policy.ts:16-25` (resolveKind) · `run.ts` (executeRunRow/capForPersist/catch-all) · `engine.ts` (walker, throw propagation) · cả 2 plan đầy đủ.
- **2 điểm consultant lo → clear SẠCH:** (1) read/write classifier ĐÚNG — `resolveKind` fallback `CONNECTOR_KIND` + unknown fail-closed-write (misclassify chỉ lệch an toàn); (2) D3 SuspendSignal sống qua catch-all (P0b T6 chặn `isSuspendSignal` trước nhánh failed).
- **1 lỗ hổng consultant KHÔNG nêu → BẮT:** 🔴F1 — idempotency chỉ ghi ở resume, KHÔNG ở initial run → double-send lần wake đầu (trace `w→d→a`). T9 nếu dựng thật sẽ FAIL. D5 cũng vô hiệu theo.

## Files changed
- `comms/active/consultant-to-cto-workflow-p0-plans.md` → **append CTO GATE** (conditional) + flip header. **GIỮ active** (cần fold-fix + ack, chưa resolved).
- `decisions/workflow-wdk-comparison-learnings.md` (+section "P0 Plans — CTO GATE").
- Checkpoint này (session 3).

## Current state — 🟢 GATED CÓ ĐIỀU KIỆN
- **D1–D7 duyệt cả 7** (D5 điều kiện-F1, D2 caveat single-process).
- **3 fix bắt buộc trước execute:** 🔴F1 wrapper idempotency initial+resume (WAL) · 🟠F2 bound tick claim (`LIMIT...FOR UPDATE SKIP LOCKED`) · 🟢 P0b T7 placeholder + dọn stale step rows.

## Next steps
- Consultant fold 3 fix vào plan docs → reply "folded" in-file → tôi liếc (không re-gate đầy đủ) → worktree → subagent P0a→P0b. T7/T9 integration phải xanh THẬT (verify F1).
- Host: USER `db:migrate` mỗi slice.

## Blockers / Risks
- **F1 là load-bearing**: nếu execute as-is → ship double-send connector (vi phạm chính mục tiêu P0). Đã chặn ở gate (rẻ hơn bắt sau 1 vòng subagent).

---

# Session 4 (cùng ngày) — CTO check tuần tự: workflow folds + eval E1+scale

## What was done
- User: "có cập nhật từ consultant lẫn eval, check tuần tự". Xử 2 luồng:
- **(1) Workflow P0 folds:** consultant reply "folded". Verify-not-prose đọc lại plan docs → F1 (`withWriteIdempotency` WAL ở `idempotency.ts`, áp initial+resume, né import-cycle) + F2 (`inArray`+`limit`+`FOR UPDATE SKIP LOCKED`) + polish ĐÚNG cả 3. **2 micro-nit:** anchor `dryRun` không có ở `run.ts:93`; import thừa `claimNode/recordNodeOutput` vào run.ts → sửa lúc execute. **🟢 GATE CLEARED.**
- **(2) Eval E0 + E1+scale plan:** E0 đã chạy host — verify `qa/eval-2026-06-06.md`: read 5/5 bền 11-tool; `write-intent-trello` flip *fabricate→won't-call* (diễn giải consultant ĐÚNG). Plan E1+scale honor 4 ràng buộc → **G1 DUYỆT**. **G2:** giữ probe-write/hoãn E2-write + **decision-trigger (curve=gate connector-GA)**. 2 fidelity-nit: no-call vs wrong-call; trello correct-schema thật.

## Files changed
- `comms/.../consultant-to-cto-workflow-p0-plans.md` → append RE-CHECK + flip ✅CLEARED → **moved resolved/**.
- `comms/.../consultant-to-cto-eval-v2-plan.md` → append GATE + flip ✅GATED → **moved resolved/**.
- `decisions/workflow-wdk-comparison-learnings.md` (+GATE CLEARED) · `decisions/eval-v2.md` (+E0 result + gate) · checkpoint này.

## Current state — cả 2 nhánh 🟢 CLEAR execute
- **Workflow P0:** worktree → P0a→P0b (sửa 2 nit lúc code); test T7/T9 xanh THẬT = backstop F1. Host: db:migrate/slice.
- **Eval:** worktree → execute; live `npm run eval`/`eval:scale` = host/user. Sau curve → write crater ~24-40 → tool-subsetting TRƯỚC connector-GA.

## Next steps
- 2 consultant: worktree → subagent-driven execute. CTO chờ: P0a/P0b test xanh thật; 2 eval scorecard (E1 + scale curve) để diễn giải.

## Blockers / Risks
- Không. 2 nhánh độc lập, parallel-safe (workflow=engine/run-layer; eval=`scripts/eval` cô lập). Cả hai gate xanh, ràng buộc honor.

---

# Session 5 (cùng ngày) — CTO consolidate: merge → main + build prod Docker

## What was done
- User: "merge toàn bộ vào main, build production docker, báo cáo". Điều tra git: phần lớn việc đã **execute thật** (không còn plan). main đã chứa eval-v2 E1+scale + harness-eval + P6 MCP + world-tools + P5 editor.
- **CHƯA ở main → merge 2 nhánh:** `worktree-workflow-p0` (P0a, 9 commit, migration 0008) + `fix/f1-write-confabulation-guard` (PR #3, write-confab guard). P0b CHƯA làm (đúng).
- Phát hiện **worktree `p0a-into-main` đang kẹt giữa merge** (UU CHANGELOG, chưa commit) — hoàn tất nó (resolve CHANGELOG = giữ CẢ Eval v2 + P0a entry) thay vì tạo merge song song.
- Merge P0a (a686f81) + f1 (b986ff7) vào main. **Verify: `tsc` sạch + `npm test` = 1117 test pass (178 file).**
- **Docker prod build:** lần đầu FAIL — `npm ci` lock-out-of-sync (`@emnapi/runtime/core` thiếu, musl/WASM). Host npm11 regen KHÔNG đủ (skew với alpine npm10.9.8). Fix đúng = **regen lock TRONG node:22-alpine** → verify `npm ci` OK (473 pkg) → commit (73e4672). **Build LẠI THÀNH CÔNG**: `laam-app:latest` 492MB, fresh.

## Files changed (main)
- Merge commits a686f81 (P0a) + b986ff7 (f1) + 73e4672 (lock fix). CHANGELOG resolved (cả 2 entry). cto checkpoint này (restore từ stash + session 5).

## Current state
- **main = efaaa92 + P0a + f1 + lock-fix**, **CHƯA push origin** (12+ commit ahead — chờ user OK; push = bước outward irreversible).
- Docker image `laam-app:latest` built (chưa chạy — agent KHÔNG `compose up`).
- Stash `cto-session-2026-06-06` còn (memory docs CTO; phần lớn đã có trên main qua branch commits).

## Next steps (HOST/USER — agent không làm được)
- `npm run db:migrate` áp **0008** (`workflow_node_idempotency`).
- **⚠️ Deploy precondition (P0a):** DRAIN run `running` TRƯỚC lần deploy P0a đầu (orphan pre-WAL không có idem row → resume có thể re-send). (CHANGELOG review #2.)
- Quyết định **push origin/main** (chờ user). Stale worktree `harness-eval`/`workflow-p0` (đã merged) có thể prune.

## Blockers / Risks
- Lock-sync là bug repo-hygiene có-từ-trước (MCP deps), đã fix cho musl. Nếu host npm11 chạy `npm install` lại có thể lệch — cân nhắc pin npm hoặc engines.

## Session 5b — push + migrate DONE (user cho phép)
- ✅ **Pushed** `origin/main` `efaaa92..c4a6eac` (14 commit). github.com:danny-exnodes/LAAM.
- ✅ **`npm run db:migrate`** chạy OK → verify: bảng `workflow_node_idempotency` (7 cột) + index `workflow_node_idem_key` (UNIQUE runId,nodeId,iterIndex) LIVE; `__drizzle_migrations`=9 (0000–0008 đã áp). Postgres `laam-v2-postgres` healthy.
- ✅ Docker image `laam-app:latest` (492MB) đã build trước đó.
- **CÒN LẠI host (user):** (1) **drain run `running` TRƯỚC khi bật app P0a mới** (precondition orphan-WAL); (2) `docker compose up -d` để chạy image mới. PR #3 (f1) có thể close (đã vào main).
