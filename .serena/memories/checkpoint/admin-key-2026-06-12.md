# Checkpoint: admin-key (access provisioning) — 2026-06-12

## What was done
- Built **owner/admin cấp access-key cho user khác** (hoàn tất phần key↔user 2 chiều của yêu cầu b).
- Design via adversarial critique workflow (4 lăng kính) → plan `docs/superpowers/plans/2026-06-12-admin-provisioned-access-keys.md` + decision `decisions/access-provisioning.md`.
- 8 tasks, TDD, trong worktree `.claude/worktrees/adminkey` (branch `feat/admin-key`):
  - T1 migration **0014** `access_token.createdByUserId`.
  - T2-4 `POST forUserId` (owner/admin, Q-B guard, Rule-13 looked-up id, tx+audit `token_issued_for`, no-store) / `GET ?userId` role-first / `DELETE` audit `token_revoked_for`.
  - T5 `laam_query_audit` principal-scope (fail-closed) — đóng rò audit-log feature này khuếch đại.
  - T6 `verifyAccessToken` tái kiểm `disabledAt`.
  - T7 UI: UsersManager keys-expander + UserKeysPanel (provision + reveal-once amber + revoke) + AccessTokensManager "provisioned by" badge + i18n vi/en/zh.
  - T8 CHANGELOG v2.4.1 + README + Serena + rbac memory đính chính + 3 backlog.
- Final adversarial review workflow (security/regression/test-quality): **READY_TO_MERGE**, 0 blocker/important, chỉ nits (đã vá 1 nit: pin Rule-13 lookup key).

## Current state
- **feat/admin-key = `230ee11` (7 commits off main 1b320dd), ĐÃ PUSH lên origin.**
- Verify: tsc exit 0; full suite **262 files / 1791 test** xanh; drizzle generate "No schema changes" (0014 + journal idx 14 coexist).
- **CHƯA merge + CHƯA tag** — bị chặn (xem Blockers).

## Next steps
- **Merge `feat/admin-key` → main + tag v2.4.1** SAU KHI việc lockfile concurrent trên main được commit. CHANGELOG sẽ cần resolve nhẹ (cả 2 thêm vào `[Unreleased]`/trước `[2.4.0]`).
- Host chạy `npm run db:migrate` (migration 0014) sau khi deploy.
- Nhắc: prod rebuild image (vẫn còn từ v2.4.0 — nền bảo mật).

## Blockers / Risks
- ⛔ **Shared main checkout có thay đổi UNCOMMITTED của agent khác** (claude-2026-06-12): vá @emnapi lockfile drift lần 5 — `package.json` (devDeps @emnapi), `package-lock.json`, CHANGELOG `[Unreleased]`, harness-lockfile-hygiene, checkpoint. **KHÔNG clobber.** Merge bị git từ chối (package.json/CHANGELOG dirty) + CHANGELOG conflict vùng `[Unreleased]`. Cần commit việc đó trước rồi mới merge v2.4.1. Feature an toàn trên branch đã push.
