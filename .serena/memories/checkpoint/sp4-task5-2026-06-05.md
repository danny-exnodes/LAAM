# Checkpoint: sp4-task5 — 2026-06-05

## What was done
- Wrote failing tests for `ToolTrace` and `Citations` (Step 1 TDD)
- Confirmed tests fail (module-not-found) before implementation (Step 2)
- Implemented `ToolTrace.tsx` — collapsible "Đã dùng N công cụ" with ✓/✗ detail list, null when empty
- Implemented `Citations.tsx` — "Nguồn: …" footer, null when empty
- All 5 tests pass (Step 4)
- `tsc --noEmit` clean — zero new type errors
- Committed as `218a1a6`

## Files changed
- `src/components/chat/ToolTrace.tsx` (new)
- `src/components/chat/Citations.tsx` (new)
- `src/components/chat/ToolTrace.test.tsx` (new)
- `src/components/chat/Citations.test.tsx` (new)

## Current state
- Task 5 complete. Both components are purely presentational, return null when empty.
- No changes to existing files; backward-compatible.
- Depends on: `toolLabel.ts` (already committed), `chat` dict SP-4 keys (already present), `I18nProvider`/`useT`.

## Next steps
- Task 6 (if any) or Task 7: wire `ToolTrace` + `Citations` into `MessageItem.tsx` (requires FE sign-off per `comms/active/sp4-to-frontend-chat-touchpoints.md`).

## Blockers / Risks
- Task 7 is gated on FE sign-off (see `comms/active/sp4-to-frontend-chat-touchpoints.md`). No blocker for Task 5 itself.
