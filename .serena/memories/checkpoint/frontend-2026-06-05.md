# Checkpoint: frontend (FE leader) — 2026-06-05

Vai trò: FE leader (sở hữu `src/components/chat/*`). Nhiệm vụ: pick-up từ lead — **FE Confirm-Card** (write-gate UI) cho SP-2.

## What was done
- Boot: đọc pickup `comms/active/lead-to-frontend-confirm-card`, spec `2026-06-05-fe-confirm-card-design`, plan (7 task). **Verify main (Rule 8):** SP-4 frame-router (`splitFrames`/`applyFrames`) + `ChatFrame.pending_write` ĐÃ có (commit `578f0ae` SP-4 + `2705f4a` SP-2/3 đã merge).
- Implement 7 task TDD trong worktree `feat/fe-confirm-card` (từ HEAD `0e690af`, node_modules junction từ main). **Additive — KHÔNG đổi backend / frames.ts / trace.ts.**

## Files (worktree feat/fe-confirm-card — 1 commit)
- `types.ts` (+`PendingWrite` + `ChatMsg.pendingWrite`) · `ConfirmCard.tsx` (mới, +`ConfirmCard.test.tsx` 6 ca) · `ChatClient.tsx` (`applyFrames` nhánh `pending_write` · `streamReply`→`streamFrom(body)` · `handleConfirm` · `setPendingStatus`) · `MessageList.tsx`/`MessageItem.tsx` (slot + `onConfirm?` optional) · `i18n/dictionaries/chat.ts` (+7 key vi/en/zh).

## Current state
- ✅ DONE + verified: `npx vitest run` = **496 pass** (102 files; +6, **0 regression** — test FE cũ xanh); `npx tsc --noEmit` = **clean**. Committed.
- Comms reply gửi lead (`lead-to-frontend-confirm-card`); backlog `agent-harness-sp2-fe-confirm` → DONE.

## Next steps
- Lead review → merge `feat/fe-confirm-card`. Manual smoke (user: server+Ollama → đề xuất write → card hiện → Xác nhận → execute + stream; Huỷ → "Đã huỷ").

## Blockers / Risks
- Round-trip (`handleConfirm`) = **glue trong ChatClient** — repo KHÔNG có `ChatClient.test` (streaming khó mock jsdom; SP-4 `applyFrames` cũng không unit-test). Verify = tsc + ConfirmCard + manual. **Flag cho lead** (đã ghi comms) — follow-up: ChatClient test-harness nếu muốn.
- ⚠️ Cross: `frames.ts` trên main = `FRAME_SEP`/`splitFrames` (canonical, SP-4); branch SP-2 backend cũ (`feat/agent-harness-sp2`) dùng `SEP` khác + thiếu `splitFrames` → **conflict khi branch đó merge** (việc của branch SP-2, không phải FE).
- Path discipline: code ở worktree, Serena/docs ở main.
