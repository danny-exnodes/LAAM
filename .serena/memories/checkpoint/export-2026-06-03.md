# Checkpoint: export — 2026-06-03

## What was done
- Wave 0 **Package E** (export utils) for v2. TDD: tests first, then implementation.
- Ported v1 semantics: CSV escaping/row-join from `public/export.js`; MD section shape from `public/chat-export.js`; JSON pretty-print; jspdf PDF.
- Conforms to LOCKED signatures: `toCsv/toMarkdown/downloadCsv/downloadJson/downloadMarkdown/downloadPdf`. Download helpers guard `typeof window !== 'undefined'`.

## Files changed (all NEW, in scope)
- `v2/src/lib/export/csv.ts` + `csv.test.ts`
- `v2/src/lib/export/markdown.ts` + `markdown.test.ts`
- `v2/src/lib/export/json.ts` + `json.test.ts`
- `v2/src/lib/export/pdf.ts` + `pdf.test.ts`
- `v2/src/lib/export/index.ts` + `index.test.ts` (re-exports + browser download helpers)
- `docs/superpowers/plans/2026-06-03-wave0-package-e-export.md` (sub-plan)

## Current state
- `npx vitest run src/lib/export` → **5 files / 27 tests passed**.
- `tsc --noEmit` → no type errors in src/lib/export.
- Uncommitted (per instructions — lead reviews/commits).

## Design notes / deviations
- Pure serializers live in csv/markdown/json/pdf.ts; the 4 `download*` browser helpers live in `index.ts` (shared private `downloadText` for the Blob+anchor dance). Matches parent file table.
- Added internal `toJson` (json.ts), `makePdfBlob`/`savePdf` (pdf.ts) beyond the locked list — these are the testable pure cores the locked download helpers delegate to. Locked public surface unchanged.
- `toMarkdown` follows the locked simpler sig (`{role,content}[]`), not v1's title/meta/attachments variant. Role labels: user→Bạn, assistant→Trợ lý, else raw role.

## Next steps
- Lead: review + commit. Integration checkpoint (run full `npm test` + `npm run build`) is coordinator's step.

## Blockers / Risks
- None.
