# Checkpoint: charts — 2026-06-03

## What was done
- Wave 2 Package W2-B (timelines + cost charts). 3 pure "use client" widgets + tests, TDD.
- Plan: docs/superpowers/plans/2026-06-03-v2-wave2-pkgB-charts.md.

## Files changed
- Added: v2/src/components/dashboard/ActivityTimeline.tsx (+ .test.tsx)
- Added: v2/src/components/dashboard/CostByModel.tsx (+ .test.tsx)
- Added: v2/src/components/dashboard/CostByProject.tsx (+ .test.tsx)
- Edited: v2/src/i18n/dictionaries/dashboard.ts — added 1 key `dash.chart.empty` (vi/en/zh).
- Added: docs/superpowers/plans/2026-06-03-v2-wave2-pkgB-charts.md

## Current state
- 18/18 tests pass (scoped vitest). tsc clean for my 3 files.
- ActivityTimeline: ComposedChart dual-axis (left bars=sessions, right line=tokens); mapActivity infers daily(MM/DD)/hourly(DD HHh) from bucket spacing.
- CostByModel: bars of costUsd by model, sorted desc, top 12, shortModel labels.
- CostByProject: stacked tokensIn/tokensOut bars per project (locked Stats["byProject"] has NO cost → charts TOKENS, label dash.chart.tokens). mapTokensByProject sorts by total desc, top 12.

## Next steps
- TL integration: compose into DashboardClient.

## Blockers / Risks
- None. recharts ResponsiveContainer is zero-size in jsdom → tests assert mappers + title/empty-state render only (per TL guidance), no SVG geometry.
- Deviation from plan: CostByProject is token-based not USD (locked type lacks cost) — flagged to TL.
