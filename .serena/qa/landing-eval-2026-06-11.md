# Đánh giá trang home (landing /) — 2026-06-11

> Nguồn: workflow 6-lens độc lập (UX/IA · Visual · Responsive · A11y · Content · Perf), 30 agent,
> mỗi finding P0/P1 được 1 agent hoài nghi verify trên code/screenshot thật. 22 confirmed, 2 refuted.
> Bằng chứng bổ sung (main session): Lighthouse mobile trên prod :3900 — A11y 89 / Best-Practices 100 / SEO 91;
> fail: color-contrast (--faint 3.69:1 ở .prog/.footerNote), meta-viewport (userScalable=no app-wide — layout.tsx:26),
> robots.txt 307 -> /login (isPublic gotcha), llms.txt (bỏ qua).
> Console: 'THREE Context Lost' khi resize = cleanup chủ động của R3F (ĐÃ BÁC, không phải bug); THREE.Clock deprecated (P2 deps).
> Screenshots baseline: D:\Projects\personal_projects\LAAM\.claude\tmp\landing-eval-shots\
> Kế hoạch xử lý: docs/superpowers/plans/2026-06-11-landing-page-improvements.md

ĐIỂM: UX 5.5 · Visual 6.5 · Responsive 4 · A11y 6 · Content 6 · Perf 7 (thang 10)

========== UX — UX & kiến trúc thông tin (product designer) — SCORE 5.5/10 ==========
SUMMARY: Trang home có khung IA 3 tầng hợp lý (hero → 6 tính năng cốt lõi quanh mech 3D → grid phụ → CTA) và hero nêu được giá trị $0/local ngay màn đầu. Nhưng lời hứa điều hướng bị phá: anchor "Cách hoạt động" (#how) trỏ vào section "Mọi thứ còn lại trong hộp" — không hề tồn tại section how-it-works; trên mobile 6 panel tính năng cốt lõi chồng đè nhau không đọc được và nav anchors biến mất hoàn toàn. Trang cũng không trả lời các câu hỏi quyết định của người xem (cài thế nào, cần phần cứng gì, có demo/ảnh sản phẩm thật không) và bỏ quên điểm khác biệt mạnh nhất của LAAM: giám sát agent mà không cần sửa agent. Scroll-hijack 380vh đặt toàn bộ selling points chính sau ~4 màn hình cuộn.
STRENGTHS:
  + Hero định vị tốt trong 1 màn hình: eyebrow giải nghĩa acronym LAAM ('Local AI Agent Monitoring'), sub nêu đủ 4 trụ (monitoring real-time / chat AI local / connectors / workflow) + 'Tất cả chạy local. Model miễn phí.' (landing.ts:17-24, desktop-1440-hero.png)
  + Có fallback nội dung đầy đủ: không WebGL hoặc prefers-reduced-motion thì 6 panel render thành grid tĩnh đọc được ngay (MechShowcase.tsx:81-94, landing.module.css:193-197) — nội dung không bao giờ bị khoá sau 3D; FeatureCard cũng force-shown khi thiếu IntersectionObserver (FeatureCard.tsx:18-21)
  + CTA flow nhất quán và auth-aware ở nav: register/login lặp ở nav + hero + footer, đã đăng nhập thì nav đổi thành 'Vào bảng điều khiển' (LandingNav.tsx:55-62, page.tsx:8-9)
  + i18n phủ đủ vi/en/zh cho mọi chuỗi kể cả nhãn a11y cho nền động và mech 3D (landing.ts:155-156); tech tokens không dịch được tách riêng vào features.ts đúng nguyên tắc
  + Exploded view desktop ở 95% bố trí 6 panel quanh mech không che nhau, đọc được (desktop-1440-mech-95.png); HUD panel có cấu trúc thông tin nhất quán: tiêu đề → telemetry → mô tả → tags
CONFIRMED:
  [ux-1|P0|conf=high] Mobile: 6 panel tính năng cốt lõi chồng đè nhau, nội dung không đọc được
     EV: landing.module.css:161-166 đặt .coHead/.coArmL/.coArmR/.coLegL/.coLegR bằng toạ độ absolute cho desktop; toàn file chỉ có 2 media query (820px cho .navLinks, 900px cho .grid) — KHÔNG có rule nào reposition .co* cho mobile. Hậu quả thấy rõ trong mobile-390-mech-50.png (panel Connectors đè lên Real-time monitoring và Local AI chat, text phía dưới bị cắt) và mobile-390-mech-95.png (6 panel xếp chồng cascade, panel Multi-machine cụt chữ, mech biến mất). Đây chính là section trình bày 6 selling points chính của platform.
     REC: Dưới ~900px bỏ exploded view pinned, render 6 HudPanel thành cột dọc như .fallbackGrid (đường code đã có sẵn ở MechShowcase.tsx:81-94 — chỉ cần thêm điều kiện viewport vào setEnable3D hoặc CSS), hoặc nếu giữ 3D thì reveal tuần tự MỖI LẦN MỘT panel ở vị trí cố định giữa màn hình.
  [ux-2|P1|conf=high] Anchor 'Cách hoạt động' (#how) trỏ sai — section how-it-works không tồn tại
     EV: LandingNav.tsx:36 render <a href="#how">{t('nav.howItWorks')}</a> với nhãn 'Cách hoạt động / How it works' (landing.ts:10), nhưng id="how" được gắn vào FeatureGrid (FeatureGrid.tsx:12) có tiêu đề 'Và hơn thế / Mọi thứ còn lại trong hộp' (landing.ts:105-106) — một grid tính năng phụ, không giải thích gì về cách hoạt động. Không file nào trong components/landing có nội dung pipeline thu thập dữ liệu.
     REC: Thêm section 'Cách hoạt động' thật (3 bước: collector zero-dep đọc transcript ~/.claude/projects → push /api/ingest bằng machine token → SSE stream ra dashboard) và gắn id="how" vào đó; FeatureGrid đổi sang id khác (vd #more). Section này đồng thời lấp lỗ hổng thông tin của ux-4.
  [ux-3|P1|conf=high] Trang không trả lời các câu hỏi quyết định: cài thế nào, chạy ở đâu, cần gì, có demo không
     EV: Toàn bộ landing.ts không có key nào về cài đặt/docs/yêu cầu hệ thống/demo; không có link tới README/GitHub ở bất kỳ component nào (Footer.tsx chỉ có CTA register + 6 tag chữ). Yêu cầu thực tế (Docker Postgres, Ollama + GPU, tesseract — README.md Quick start) hoàn toàn vắng mặt. 'Screenshot' trong HUD panel là giả — chỉ là icon + scanline + grid (HudPanel.tsx:31-43, class .shot, aria-hidden) — cả trang không có một ảnh UI sản phẩm thật nào; số máy hỗ trợ chỉ ghi 'all' (features.ts:82).
     REC: Phục vụ đúng mục tiêu 'show nhiều thông tin hơn': (1) thay faux screenshot trong 1-2 HUD panel bằng ảnh dashboard/chat thật, (2) thêm khối 'Bắt đầu trong 3 bước' (docker compose up → npm run dev → chạy collector) kèm yêu cầu phần cứng, (3) footer thêm link README/tài liệu.
  [ux-4|P1|conf=high] Điểm khác biệt mạnh nhất — giám sát KHÔNG cần sửa agent — vắng mặt hoàn toàn trong copy
     EV: Định vị cốt lõi của LAAM là 'watch Claude agents with no changes to the agents themselves — reads JSONL transcripts from ~/.claude/projects' (CLAUDE.md Project Overview), nhưng landing.ts không có chuỗi nào nói điều này: hero.sub chỉ nói 'theo dõi real-time', feat.1.desc chỉ nhắc SSE, feat.5.desc nhắc collector nhưng không nói zero-instrumentation. Người xem lần đầu sẽ hỏi 'phải gắn SDK/sửa agent à?' và trang không trả lời.
     REC: Thêm vào hero.sub hoặc feat.1.desc: 'không cần sửa một dòng nào trong agent — LAAM tự đọc transcript Claude Code'. Đây là claim trust rẻ nhất mà thuyết phục nhất của platform.
  [ux-5|P1|conf=high] Scroll-hijack 380vh: 6 selling points chính bị khoá sau ~4 màn hình cuộn
     EV: .explode cao 380vh với .sticky pin 100vh (landing.module.css:118-119); panel reveal từ progress 0.12 đến 0.86 (features.ts:59-89) — người dùng phải cuộn gần 4 viewport mới đọc đủ 6 tính năng, và click nav '#features' chỉ rơi vào heading ở 0% (MechShowcase.tsx:98-101) rồi vẫn phải cuộn hết. Không có nút skip; chỉ báo tiến độ .prog chỉ hiện % (MechShowcase.tsx:115). Nghịch lý: bản fallback reduced-motion lại scan được cả 6 panel ngay lập tức. Console còn ghi nhận 'THREE.WebGLRenderer: Context Lost' khi resize — khi 3D chết, người dùng vẫn trả nguyên chi phí cuộn 380vh chỉ để xem panel.
     REC: Giảm chiều cao xuống ~250vh và dồn revealAt sớm hơn (panel cuối ≤0.7); cân nhắc panel đã hiện thì giữ lại dạng danh sách rút gọn, hoặc thêm liên kết 'Xem tất cả tính năng ↓' nhảy qua vùng pin.
  [ux-6|P1|conf=high] Mobile mất toàn bộ điều hướng section — navLinks ẩn dưới 820px không có thay thế
     EV: landing.module.css:93 `@media (max-width: 820px) { .navLinks { display: none; } }` và LandingNav.tsx không render menu/hamburger nào khác; mobile-390-hero.png và tablet-768-hero.png xác nhận nav chỉ còn logo + lang switcher + Sign in + Get started. Người dùng mobile/tablet dọc không thể nhảy tới Tính năng/Công nghệ.
     REC: Thêm hamburger đơn giản hoặc tối thiểu giữ lại 1 link 'Tính năng' trên mobile; tablet 768px đủ rộng để giữ nguyên 3 link nếu rút gọn padding.
REFUTED:
P2:
  [ux-7] Anchor 'Công nghệ' (#stack) trỏ vào footer CTA — nội dung chỉ là 6 tag chữ — Hoặc nâng cấp thành section stack riêng (mỗi tag 1 dòng giải thích vai trò — vd 'Ollama: model local $0 trên GPU của bạn'), hoặc bỏ anchor 'Công nghệ' khỏi nav nếu không định đầu tư nội dung.
  [ux-8] Hero/Footer CTA không nhận biết trạng thái đăng nhập — user đã có account vẫn bị mời 'Bắt đầu' → /register — Truyền isAuthed xuống Hero/Footer: đã đăng nhập thì CTA chính thành 'Vào bảng điều khiển' (/dashboard), key i18n nav.dashboard đã có sẵn.
  [ux-9] Hero thiếu định vị 'cho ai' và tiêu đề tiếng Việt gãy ngữ pháp; telemetry giả dễ gây hiểu nhầm — Viết lại title VI thành câu tự nhiên (vd 'Nhìn thấy mọi agent của bạn — sống động, real-time'); thêm 1 dòng 'dành cho team dev tự host' vào hero.sub hoặc eyebrow. Với telemetry: hoặc ghi chú nhỏ 'minh hoạ', hoặc thay bằng số thật từ /api/stats nếu instance đang chạy.

========== VIS — visual-design (art director) — SCORE 6.5/10 ==========
SUMMARY: Landing có ngôn ngữ thị giác "PS5 Aurora" đặc sắc, nhất quán và khá premium ở desktop: hero typography phân cấp chuẩn, background nhiều lớp (key light + shaft + dot field + vignette) tạo chiều sâu, HUD panel được art-direct chi tiết tới từng tick/gauge. Tuy nhiên trang đang bán "vibe" thay vì bán sản phẩm: không có một screenshot thật nào của app (khung .shot là giả + telemetry hardcode), mật độ thông tin thấp giữa các section rất rỗng, và khoảnh khắc cao trào (mech tháo rời 95%) bị vỡ bố cục — panel chồng đè nhau trên desktop và vỡ hẳn trên mobile (chữ bị cắt, panel chui dưới nav). Palette landing lệch tông teal Matte Dark của app nhưng giữ đúng accent #36a6d6 nên chấp nhận được ở mức marketing page.
STRENGTHS:
  + Aesthetic nhất quán, có chủ đích: background stack 4 lớp (base gradient + key light + shaft + dot canvas + vignette, landing.module.css:29-48) cho cảm giác console boot-screen premium — desktop-1440-hero.png đọc rất sạch.
  + Phân cấp typography hero mẫu mực: eyebrow tracking 0.32em → H1 gradient weight-200 clamp(2.6rem→5rem) với dòng accent đậm hơn → sub muted → CTA pair → scroll cue (landing.module.css:96-109, Hero.tsx:10-25). Một trục thị giác trung tâm rõ ràng.
  + HUD panel được art-direct kỹ và đồng bộ ở mọi instance: khung clip-path góc cắt + viền gradient phát sáng, cột tick trái, mod-id mono, telemetry 3 ô, tag mono, conic gauge (landing.module.css:125-158, HudPanel.tsx) — desktop-1440-mech-30.png trông như UI game thật.
  + Giữ đúng accent hệ thống #36a6d6 của Matte Dark (landing.module.css:5 khớp decisions/matte-dark-redesign.md) — brand continuity giữa landing và app được bảo toàn ở cấp màu nhấn.
  + Có đường lui tử tế: fallback grid khi không có WebGL/reduced-motion (MechShowcase.tsx:81-94) và toàn bộ animation tắt theo prefers-reduced-motion (landing.module.css:193-197).
  + Footer tái sử dụng chip .tag cho stack row (Footer.tsx:18-22) — chi tiết nhỏ nhưng giữ ngôn ngữ thị giác xuyên suốt trang.
CONFIRMED:
  [vis-1|P0|conf=high] Mech section trên mobile vỡ bố cục: panel chồng thành 'chồng bài', tiêu đề bị cắt, panel đầu chui dưới nav
     EV: mobile-390-mech-95.png: 4+ panel đè lên nhau, tiêu đề bị che cụt ('Conne…', 'Mu…'), không panel nào đọc trọn vẹn. mobile-390-mech-50.png: panel 'Real-time monitoring' (coHead, top 6%) bị nav fixed che mất phần đầu. Nguyên nhân trong code: placement absolute theo % desktop tại landing.module.css:161-166 (.coHead/.coArmL/.coLegL…) KHÔNG có media query mobile nào; .hudFrame max-width 340px (landing.module.css:126) gần bằng cả viewport 390px nên mọi vị trí trái/phải/giữa đều trùng nhau.
     REC: Dưới ~900px bỏ hẳn layout absolute quanh mech: chuyển sang chế độ 'một panel mỗi bước' — panel đang active hiện full-width dưới mech thu nhỏ, panel trước fade out (đang có sẵn revealAt trong features.ts để điều phối), hoặc đơn giản nhất là dùng luôn .fallbackGrid (đã có sẵn, landing.module.css:122) cho mobile. Đồng thời chừa offset top = chiều cao nav cho panel coHead.
  [vis-2|P1|conf=high] Desktop progress 95%: 6 panel hiện đồng thời chồng lấn nhau và che kín mech — khoảnh khắc cao trào trông rối nhất trang
     EV: desktop-1440-mech-95.png: cột giữa panel head (top 6%) và core (bottom 5%) giao nhau; cạnh trái 'Connectors' (coArmL top 24%) đè lên 'Multi-machine' (coLegL bottom 12%); mech 3D gần như biến mất sau panel. Code: landing.module.css:161-166 đặt 6 panel bằng % cố định trong .sticky 100vh, không có cơ chế tránh va chạm — ở viewport cao ~900px, panel cao ~400px tất yếu giao nhau theo trục dọc.
     REC: Giới hạn số panel hiển thị đồng thời (fade panel đã đọc về opacity ~0.25/scale 0.9 khi panel sau reveal), hoặc scale toàn cụm theo vh (panel nhỏ lại khi viewport thấp), hoặc kết thúc sequence bằng trạng thái 'tổng kết' xếp 6 panel vào lưới 3×2 gọn gàng thay vì để chúng đè tự do.
  [vis-3|P1|conf=high] Khung '.shot' là screenshot GIẢ (scanline + icon) — chiếm diện tích vàng của panel và làm giảm độ tin cậy sản phẩm
     EV: HudPanel.tsx:31-43 render khung giả lập cửa sổ trình duyệt (3 chấm + path 'laam // {id}') nhưng bên trong chỉ có scanline + lưới + 1 icon lucide 40px (landing.module.css:141-148, cao 116px). Cộng hưởng: telemetry hardcode giả live-data (features.ts:58 '42', '4m 12s'; gauge 99%) và status chip 'ONLINE'. Với người xem là dev nội bộ, khung mimic-screenshot-nhưng-rỗng phát tín hiệu 'sản phẩm chưa tồn tại' — trong khi LAAM v2 đã ship đầy đủ dashboard/chat/graph.
     REC: Thay icon bằng crop screenshot THẬT của app cho từng feature (dashboard cho MOD-01, /chat cho MOD-02, /connectors cho MOD-03, /graph cho MOD-06…) — một thay đổi vừa tăng trust vừa tăng mật độ thông tin đúng yêu cầu user. Nếu chưa có asset, vẽ micro-mockup trung thực (mini sparkline, hàng session row) thay vì khung browser rỗng; hoặc bỏ phần chrome 3 chấm để không gợi ý đây là screenshot.
  [vis-4|P1|conf=high] Mật độ thông tin thấp: không có hình sản phẩm thật, các section thưa và nhiều khoảng rỗng lớn
     EV: Cả trang không có một screenshot app nào. Hero chiếm 100dvh nhưng chỉ ~40 từ (landing.module.css:96, desktop-1440-hero.png). Section explode cao 380vh cho 6 panel ngắn (landing.module.css:118). Footer padding 14vh/16vh (landing.module.css:185) — desktop-1440-footer.png cho thấy dải trống rất lớn giữa grid và footer title. Nội dung bán hàng chỉ gồm 6 panel + 6 card 2 dòng + 6 chip stack, trong khi README liệt kê nhiều hơn hẳn (map, OCR vie/eng/chi, audit, multi-machine collector…).
     REC: Thêm 1 section 'product shot' (ảnh dashboard thật trong khung thiết bị, đặt giữa hero và mech hoặc sau mech), 1 dải 'by the numbers' (7 connectors · 3 ngôn ngữ · $0 model · n máy · 1125 tests) tận dụng style .telem sẵn có, và rút padding .section/.footer (12vh→8vh, 14-16vh→8-10vh). Mở rộng FeatureGrid từ 6 lên 8-9 card với các tính năng đã ship trong README (bản đồ/geocode, OCR, timeline agent).
REFUTED:
P2:
  [vis-5] Nav label không khớp nội dung anchor: 'How it works' trỏ vào grid 'Everything else in the box', 'Stack' trỏ vào footer CTA — Hoặc đổi label nav cho đúng ('Tính năng khác', 'Công nghệ'), hoặc — tốt hơn cho mục tiêu show nhiều thông tin — xây section 'How it works' thật (sơ đồ 3 bước: collector đọc transcript → ingest vào Postgres → SSE ra dashboard) làm anchor #how.
  [vis-6] Palette landing lệch tông Matte Dark của app: nền xanh-dương đại dương vs teal #001616, dùng translucency + glow trong khi app cấm gloss — Mức độ expressive cao hơn cho marketing page là chấp nhận được — nhưng nên kéo base hue về họ teal/cyan của app (hoặc pha teal vào gradient nền) để cú chuyển sang /login–/register (AuthShell đã cyan-hoá) không bị đổi tông đột ngột; cân nhắc dùng chung token --accent/--bloom từ globals để hai thế giới chia sẻ DNA.
  [vis-7] Micro-typography trong HUD panel dưới ngưỡng đọc thoải mái (9px) và không scale theo viewport — Nâng sàn micro-type lên 10–11px (giữ được chất HUD), riêng .statK/.shotPath tối thiểu 10px; cân nhắc clamp() nhẹ để chữ không co thêm tương đối trên màn nhỏ.
  [vis-8] Nav mobile thiếu polish: nút pill 'Get started'/'Sign in' wrap 2 dòng, navLinks biến mất không có thay thế — Thêm white-space: nowrap + giảm padding/font cho .btnPrimary/.btnGhost trong nav ở breakpoint nhỏ (hoặc gộp còn 1 CTA duy nhất trên mobile); thêm menu thu gọn đơn giản cho 3 section link.

========== RSP — responsive engineer — SCORE 4/10 ==========
SUMMARY: Landing page có nền tảng fluid tốt (clamp() cho typography/padding, 100dvh ở root/hero, fallback không-WebGL dạng grid, tôn trọng prefers-reduced-motion) nên hero, feature grid và footer hiển thị ổn ở 390px. Nhưng toàn bộ CSS chỉ có đúng 2 media query (820px ẩn navLinks, 900px grid 1 cột) — section trung tâm "mech exploded" dùng 6 panel absolute KHÔNG có layout mobile nên chồng đè hỗn loạn, không đọc được ở 390px lẫn 768px (P0). Nav mobile không có hamburger (trái convention dự án md=768), nút Sign in/Get started wrap 2 dòng, .sticky dùng 100vh lệch với dvh, WebGL context lost khi resize không được xử lý. Breakpoint cần bổ sung: ~1100px (chuyển mech panels sang layout tuần tự), 768px (hamburger, hợp nhất 820/900 về md), 640px (nav CTA, grid 2 cột), ~480px (HUD panel width fluid) + safe-area-inset cho phần tử sát đáy.
STRENGTHS:
  + Typography/padding fluid bằng clamp() xuyên suốt (.h1 clamp(2.6rem,6.4vw,5rem), .wrap padding clamp(20px,5vw,56px), .secTitle, .footerTitle) — hero scale đẹp từ 390px đến 1440px (mobile-390-hero.png, desktop-1440-hero.png)
  + Root dùng min-height:100dvh + overflow-x:clip có comment giải thích vì sao không dùng overflow hidden (giữ position:sticky) — landing.module.css:16-20
  + Progressive enhancement đúng: không WebGL hoặc prefers-reduced-motion thì render fallbackGrid auto-fit đọc được mọi panel (MechShowcase.tsx:81-94); reduced-motion cũng tắt animation CSS (landing.module.css:193-197)
  + 3D lazy-load qua next/dynamic ssr:false + IntersectionObserver + frameloop='never' khi ra khỏi viewport (MechShowcase.tsx:14,63-69, Mech3D.tsx:37) — tiết kiệm pin/CPU mobile
  + Feature grid 1 cột và footer hiển thị tốt ở 390px (mobile-390-grid.png, mobile-390-footer.png); dot-field canvas tự giới hạn DPR≤2 và số hạt theo diện tích (useDotField.ts:59,64)
CONFIRMED:
  [rsp-1|P0|conf=high] Mech exploded section: 6 HUD panel absolute không có layout mobile/tablet — chồng đè hỗn loạn, không đọc được
     EV: landing.module.css:161-166 (.coHead/.coCore/.coArmL/.coArmR/.coLegL/.coLegR đều position:absolute với left/right %, KHÔNG có media query nào điều chỉnh — toàn file chỉ có 2 media query ở dòng 93 và 172). Panel .hudFrame max-width:340px (dòng 126); coArmL left:3% + coArmR right:3% cần ≥ ~1100px mới không giao nhau. Screenshot: mobile-390-mech-50.png (panel Connectors đè lên panel Real-time monitoring và mech), mobile-390-mech-95.png (4+ panel xếp chồng, chữ bị cắt giữa card, gauge bị che), tablet-768-mech-60.png (768px vẫn chồng đè 3 panel giữa màn hình). Đây là section trình diễn 6 tính năng cốt lõi — trên mobile/tablet hoàn toàn không truyền tải được thông tin.
     REC: Thêm @media (max-width: 1100px) cho chế độ pinned: bỏ placement absolute 6 góc, chuyển sang hiển thị TUẦN TỰ — mech ở nửa trên stage, panel hiện tại (theo progress/revealAt) ở nửa dưới dạng 1 cột căn giữa với width min(340px, calc(100vw - 32px)), fade-out panel cũ khi panel mới reveal (state đã có sẵn qua progress >= f.revealAt trong MechShowcase.tsx:108). Phương án rẻ hơn: dưới 1100px render nhánh fallbackGrid (MechShowcase.tsx:81-94) thay vì exploded view — code đã có sẵn, chỉ cần thêm điều kiện matchMedia vào setEnable3D.
  [rsp-2|P1|conf=high] Nav mobile không có hamburger — navLinks bị display:none, mất điều hướng anchor; sai convention dự án (md=768)
     EV: landing.module.css:93 (@media max-width:820px { .navLinks { display:none } }) — link #features/#how/#stack biến mất hoàn toàn dưới 820px, không có menu thay thế (LandingNav.tsx:34-38). Trái với .serena/memories/decisions/responsive-conventions.md: "Breakpoint chính: md (768px) là ngưỡng gập nav. Mobile nav = hamburger... Dưới md: ẩn nav ngang + actions, hiện nút ☰. Dropdown đặt absolute top-full". Breakpoint 820px cũng lệch chuẩn md=768 của dự án. mobile-390-hero.png xác nhận chỉ còn brand + lang + 2 nút.
     REC: Đổi breakpoint 820px → 768px cho thống nhất. Thêm nút hamburger (useState open, dropdown absolute top-full overlay theo đúng pattern app-header.tsx) chứa 3 anchor link + nút Đăng nhập; link tự đóng menu onClick.
  [rsp-3|P1|conf=high] Nút nav 'Sign in' và 'Get started' wrap 2 dòng ở 390px — nav chật, nhìn vỡ
     EV: mobile-390-hero.png: cả hai nút pill đều bị bẻ làm 2 dòng ('Sign / in', 'Get / started'), nav chiếm ~2 hàng chiều cao. Nguyên nhân: .btnPrimary/.btnGhost (landing.module.css:59-71) không có white-space:nowrap, padding 13px 24-26px cố định; hàng nav ở 390px phải chứa brand + langGroup (3 nút) + 2 CTA (LandingNav.tsx:40-63) nên flex bóp chữ xuống dòng.
     REC: Thêm white-space:nowrap cho .btnPrimary/.btnGhost. Dưới 768px (cùng media query hamburger): ẩn nút Sign in ghost khỏi navRight (đưa vào menu hamburger), giảm padding CTA còn ~9px 16px, font-size 13px. Cân nhắc thu langGroup thành 1 nút ngôn ngữ hiện tại + dropdown.
  [rsp-4|P1|conf=high] .sticky dùng height:100vh trong khi root/hero dùng dvh — stage cao hơn viewport thật trên mobile, panel đáy và progress bị che
     EV: landing.module.css:119 (.sticky { height: 100vh }) lệch với :16 và :96 (100dvh). Trên mobile browser, 100vh = viewport lúc URL bar ẩn → khi URL bar đang hiện, đáy stage (gồm .coCore bottom:5% dòng 162 và .prog bottom:20px dòng 121) nằm dưới browser chrome; mobile-390-mech-95.png cho thấy panel dưới cùng sát mép/tràn đáy. Mix vh/dvh còn gây nhảy layout khi URL bar collapse lúc cuộn qua section pinned.
     REC: Đổi .sticky sang height:100svh (ổn định khi cuộn, không nhảy như dvh) hoặc 100dvh; thêm padding-bottom calc(20px + env(safe-area-inset-bottom)) cho .prog (cần viewport-fit:cover đã quy định trong responsive-conventions.md).
REFUTED:
  [rsp-5] THREE 'Context Lost' khi resize/đổi hướng màn hình — không có handler khôi phục, canvas có thể đen vĩnh viễn — note: Log 'Context Lost' khi resize là cleanup chủ động: MechShowcase.tsx:105 mount Canvas theo IntersectionObserver, khi unmount R3F gọi gl.forceContextLoss() (events-b389eeca.esm.js:15873) rồi remount tạo context MỚI — không thể đen vĩnh viễn. Hơn nữa three.js tự đăng ký webglcontextlost/restored với preventDefault + initGLContext (three.module.js:16323-16324, 17033-17063), nên claim 'không có handler khôi phục' sai ở tầng thư viện; không có screenshot/console log nào chứng minh trạng thái canvas đen.
P2:
  [rsp-6] Parallax chỉ có mousemove — không có tương đương cho touch, mech và starfield tĩnh trên mobile — Lái parallax trên touch bằng scroll progress sẵn có (progressRef) — map progress sang pointerRef.y; hoặc dùng pointermove (gộp mouse+touch) / deviceorientation có permission gate. Đây là enhancement, ưu tiên sau khi sửa rsp-1.
  [rsp-7] fallbackGrid minmax(280px,1fr) tràn ngang ở viewport < 320px, phần tràn bị overflow-x:clip cắt mất — Đổi thành repeat(auto-fit, minmax(min(280px, 100%), 1fr)) — một thay đổi 1 dòng, an toàn mọi viewport.
  [rsp-8] Starfield rebuild toàn bộ hạt mỗi lần resize — trên iOS thanh URL co giãn phát resize làm nền 'nhảy' ngẫu nhiên giữa lúc cuộn — Chỉ rebuild khi width thay đổi (bỏ qua resize chỉ đổi height — dấu hiệu URL bar), hoặc scale lại tọa độ hạt theo tỉ lệ kích thước mới thay vì random lại.
  [rsp-9] Touch target nút ngôn ngữ quá nhỏ (~22-24px cao) trên nav mobile — Trong media query mobile: tăng padding langBtn lên ~10px 12px (hoặc min-height 40px), hoặc chuyển bộ chọn ngôn ngữ vào menu hamburger.
  [rsp-10] Grid phụ nhảy thẳng 3 cột → 1 cột ở 900px — tablet 768px chỉ 1 cột card rất rộng, thiếu bước 2 cột — Dùng 2 bước: @media (max-width: 1024px) → 2 cột, @media (max-width: 640px) → 1 cột; nhân tiện hợp nhất toàn bộ breakpoint của file về bộ chuẩn dự án (640/768/1024 + 1100 cho mech).

========== A11Y — a11y auditor (WCAG 2.1 AA) — SCORE 6/10 ==========
SUMMARY: Nền tảng a11y tốt hơn mặt bằng chung landing page: landmark/heading chuẩn, lang switcher có role=group + aria-pressed, focus-visible được style, và đường fallback reduced-motion/no-WebGL đầy đủ (lưới HudPanel tĩnh thay toàn bộ scroll 3D). Contrast tầng chính vượt AA rõ rệt (--muted 7.29:1, tag #bfe3ff 10.5:1, status #aef0c8 13.0:1, langActive 6.64:1 — đều pass). Tuy nhiên có 1 lỗi chặn: trên mobile 390px và tablet 768px các panel HUD đặt absolute theo toạ độ desktop (không có media query) nên chồng đè hoàn toàn lên nhau — fail WCAG 1.4.10 Reflow, người dùng mobile không đọc được 6 tính năng cốt lõi. Hai lỗi P1: tầng màu --faint chỉ đạt 3.61–3.70:1 (fail 1.4.3 trên footerNote 13px, prog 11px, label telemetry 9px) và <html lang> không được cập nhật khi đổi ngôn ngữ client-side (fail 3.1.1). Còn lại là polish: canvas 3D chưa đánh dấu decorative (key i18n a11y.mech đã soạn sẵn nhưng chưa dùng), accessible name nút ngôn ngữ chỉ là mã 2 ký tự, nav sections biến mất ≤820px không có thay thế.
STRENGTHS:
  + Landmark + heading chuẩn: header / nav[aria-label="Sections"] / main / footer (Landing.tsx:17-25, LandingNav.tsx:34); thứ tự h1 (Hero) → h2 (secTitle) → h3 (HudPanel/FeatureCard) không nhảy bậc trên cả hai nhánh render.
  + Fallback reduced-motion/no-WebGL trọn vẹn: MechShowcase.tsx:47-50 + 81-94 thay toàn bộ section 3D pinned bằng lưới HudPanel tĩnh; useDotField.ts:31,115-116 chỉ vẽ 1 frame tĩnh, không chạy rAF loop; landing.module.css:193-197 tắt mọi keyframe trang trí (key, scanline, statusDot, eyebrow, scrollBar) và ép fcard/coHidden hiện sẵn.
  + Contrast tầng chữ chính vượt chuẩn (đã tính): --text #e8f1fb = 17.37:1, --muted = 7.29:1 trên #000b16 và 6.77:1 trên nền HUD (pass cả AAA); tag #bfe3ff = 10.5–12.7:1; status #aef0c8 = 13.02:1; langActive #03171b/#36a6d6 = 6.64:1; nút primary #062033 = 12.06:1 — tất cả pass AA.
  + Lang switcher đúng ngữ nghĩa: role="group" + aria-label="Ngôn ngữ" + aria-pressed trên từng nút (LandingNav.tsx:41-52); focus-visible custom 2px #7fe0ff cho btnPrimary/btnGhost/langBtn/navLink (landing.module.css:72-74).
  + Mọi lớp trang trí đều aria-hidden đúng: aurora scene (AuroraBackground.tsx:15), scrollcue (Hero.tsx:22), ticks/statusDot/faux-screenshot trong HudPanel (HudPanel.tsx:16,21,31), icon FeatureCard (FeatureCard.tsx:37).
  + Không có bẫy bàn phím ở section pinned 380vh: HudPanel không chứa phần tử focusable nào; panel coHidden (opacity:0, vẫn trong DOM) vẫn nằm trong accessibility tree nên screen reader đọc được đủ 6 panel mà không phụ thuộc tiến độ scroll — nội dung không bị khoá sau tương tác thị giác.
CONFIRMED:
  [a11y-1|P0|conf=high] Mobile/tablet: panel HUD chồng đè nhau hoàn toàn — fail WCAG 1.4.10 Reflow, chặn đọc 6 tính năng cốt lõi
     EV: Screenshot mobile-390-mech-50.png và mobile-390-mech-95.png: 3-4 panel xếp đè trực tiếp lên nhau, panel dưới bị che gần hết chữ, panel trên cùng tràn dưới nav; tablet-768-mech-60.png cũng chồng đè. Nguyên nhân trong code: .coHead/.coCore/.coArmL/.coArmR/.coLegL/.coLegR đặt absolute theo toạ độ desktop (landing.module.css:161-166, comment ghi rõ "(desktop)") và KHÔNG có bất kỳ media query nào reposition cho viewport hẹp; .sticky overflow:hidden (dòng 119) cắt luôn phần tràn. Mobile có WebGL nên đi nhánh 3D đầy đủ (MechShowcase.tsx:81 chỉ fallback khi !enable3D), không bao giờ nhận lưới đọc được. Lỗi này cũng tái hiện trên desktop khi zoom 400% (viewport hiệu dụng ~360px) — đúng phạm vi 1.4.10.
     REC: Thêm breakpoint (vd ≤900px): bỏ position absolute cho 6 panel, render section mech thành flow tĩnh — đơn giản nhất là tái dùng .fallbackGrid (đã có sẵn, đã pass mọi kiểm tra) cho viewport hẹp, giữ exploded view chỉ cho desktop. Đồng thời đây là chỗ "show nhiều thông tin hơn" rẻ nhất: mobile hiện tại đang giấu nội dung thay vì khoe.
  [a11y-2|P1|conf=high] Tầng màu --faint fail contrast 1.4.3 trên text thật (3.61–3.70:1 < 4.5:1)
     EV: Đã tính ratio: --faint rgba(232,241,251,.42) trên #000b16 = 3.70:1. Áp dụng cho text nội dung thật: .footerNote 13px "$0 local model · self-hosted · internal tool" (landing.module.css:188, thấy rõ trong desktop-1440-footer.png/mobile-390-footer.png); .prog 11px hiển thị "% tiến độ" trong section mech (dòng 121, MechShowcase.tsx:115); .statK label telemetry 9px composite trên nền .stat = 3.61:1 (dòng 150-151) — đây là label của số liệu thật (SERVICES/CRYPTO/WRITES...). .scrollcue 11px (dòng 106) cũng 3.70:1 — aria-hidden không miễn trừ 1.4.3 vì text vẫn hiển thị thị giác. Ngưỡng AA normal-text là 4.5:1.
     REC: Nâng --faint từ .42 lên ≥.56 (đạt ~4.9:1) hoặc đổi các text nội dung thật (footerNote, prog, statK) sang --muted; giữ .42 chỉ cho phần tử thuần trang trí bên trong vùng aria-hidden (shotPath). Đồng thời cân nhắc nâng statK từ 9px lên ≥10px.
  [a11y-3|P1|conf=high] <html lang> không cập nhật khi đổi ngôn ngữ client-side — fail WCAG 3.1.1
     EV: src/i18n/provider.tsx:17-20: setLang chỉ gọi setActive(l) + writeLangCookie(l), không đụng document.documentElement.lang. layout.tsx:41 chỉ set lang lúc SSR từ cookie. Hệ quả: người dùng vi bấm "zh" → toàn bộ text đổi sang tiếng Trung nhưng <html lang="vi"> giữ nguyên đến khi reload — screen reader đọc 中文 bằng giọng tiếng Việt (không phát âm được), switch trong LandingNav.tsx:48 là live không reload.
     REC: Trong setLang (provider.tsx) thêm document.documentElement.lang = l (guard typeof document). Sửa 1 dòng, ảnh hưởng toàn app vì provider dùng chung.
REFUTED:
P2:
  [a11y-4] Canvas 3D Mech không được đánh dấu decorative / không có accessible name — key i18n a11y.mech đã soạn nhưng không dùng — Vì 6 HudPanel đã là text alternative đầy đủ, gắn aria-hidden="true" lên div .stageWrap (MechShowcase.tsx:104) là đủ và đúng nhất; hoặc dùng key a11y.mech làm aria-label với role="img" nếu muốn SR biết có mô hình 3D. Xoá key thừa nếu chọn aria-hidden.
  [a11y-5] Nút chuyển ngôn ngữ: accessible name chỉ là mã 2 ký tự, target ~22px cao — Thêm aria-label bằng tên bản địa: aria-label="Tiếng Việt"/"English"/"中文" (và lang tương ứng trên label nếu render text); role=group + aria-label "Ngôn ngữ" đã có sẵn nên chỉ cần sửa từng nút. Tăng padding lên ≥7px dọc để chạm mốc 24px.
  [a11y-6] Nav sections biến mất ở ≤820px không có thay thế — Mức tối thiểu: giữ các anchor link dưới dạng menu thu gọn (details/summary hoặc nút mở overlay) — vì chỉ là 3 anchor, một hàng link nhỏ thứ hai dưới nav cũng đủ và rẻ hơn hamburger.
  [a11y-7] Reduced-motion chỉ đánh giá một lần lúc mount + vài chi tiết focus/motion lẻ — Thêm listener mql.addEventListener('change', ...) ở 2 chỗ (hoặc chấp nhận trade-off và ghi chú); thêm .brand vào rule focus-visible; đưa transition transform của 2 nút vào khối @media (prefers-reduced-motion: reduce). Đều là sửa vài dòng.

========== CONTENT — content strategist / marketing — SCORE 6/10 ==========
SUMMARY: Copy hiện tại được viết gọn, có giọng điệu riêng (sci-fi HUD) và phủ được 6 trụ cột chính (SSE monitoring, chat $0, connectors, workflow, multi-machine, dashboard) + 6 card phụ (graph, RBAC, local-first, audit, i18n, world tools), với parity vi/en/zh khá tốt và footer trung thực. Tuy nhiên trang đang BỎ LỠ mục tiêu "show nhiều thông tin để người xem nhận ra điểm mạnh": nav hứa "Cách hoạt động" nhưng không có section how-it-works (anchor #how trỏ nhầm vào grid "And more"); telemetry trong HUD là số tĩnh giả ('42', '4m 12s') trình bày như số thật; nhiều tính năng thật bị thiếu hẳn (Search full-text, map/geo tools, OCR 3 ngữ, cancellable workflow runs); "screenshot" trong panel là faux (chỉ icon + scanline) thay vì ảnh sản phẩm thật; không có section security/privacy pitch, so sánh chi phí $0 vs cloud, hay FAQ. Một vài mâu thuẫn nhỏ làm giảm độ tin với audience dev: "7 LINKED" nhưng desc chỉ nêu 6 dịch vụ; model hardcode 'qwen3-vl' trong khi README nói primary là gemma4:e4b. Điểm 6/10: nền tảng copy tốt nhưng độ phủ thông tin ~60% năng lực thật của platform.
STRENGTHS:
  + Parity i18n 3 ngữ gần như đầy đủ cho mọi key (src/i18n/dictionaries/landing.ts) — copy en tự nhiên, có idiom tốt ('one pane of glass', 'No cloud bill'), zh dịch chỉn chu; đúng cam kết i18n vi/en/zh của dự án.
  + Differentiator '$0 / local-first / dữ liệu không rời máy' được lặp nhất quán xuyên trang: hero.sub (landing.ts:21), feat.2 ($0 · GPU), grid.local (landing.ts:119-124), footer.note ('$0 local model · self-hosted · internal tool', landing.ts:152) — đây là pitch mạnh nhất và trang đã nắm đúng.
  + Footer note trung thực, không over-claim ('công cụ nội bộ' — landing.ts:152, thấy rõ trong desktop-1440-footer.png) — phù hợp audience nội bộ.
  + Kiến trúc nội dung sạch: prose nằm trong i18n dict, token kỹ thuật universal (SSE, AES-256, OAuth, cron) nằm trong features.ts (comment landing.ts:1-4) — dễ thêm section/ngôn ngữ mới.
  + 6 core feature + 6 grid card phủ được phần lớn trụ cột: real-time SSE, chat đa phương thức $0 (web/OCR/vision/tool-calling), connectors mã hoá per-user + write gated, workflow + scheduler, collector zero-dep, dashboard cost/token/leaderboard, RBAC 4 role, audit log, graph, i18n.
  + CTA auth-aware (nav đổi thành 'Vào bảng điều khiển' khi đã đăng nhập — LandingNav.tsx:55-62) và fallback không-WebGL vẫn render đủ 6 panel nội dung (MechShowcase.tsx:79-94) — thông tin không bao giờ bị mất.
CONFIRMED:
  [cnt-1|P1|conf=high] Nav hứa 'Cách hoạt động' nhưng không tồn tại section how-it-works — anchor #how trỏ nhầm vào grid 'And more'
     EV: LandingNav.tsx:36 (`<a href="#how">{t('nav.howItWorks')}</a>`) trong khi `id="how"` được gắn vào FeatureGrid (FeatureGrid.tsx:12) — section có heading 'Everything else in the box' (desktop-1440-grid.png). Key 'nav.howItWorks' (landing.ts:10) tồn tại nhưng không có bất kỳ key/section how.* nào trong dict.
     REC: Thêm section How-it-works 3 bước đúng flow thật (đối chiếu README): (1) chạy collector zero-dep trên mỗi máy dev → /api/ingest, (2) đăng ký/đăng nhập (user đầu tiên là owner), (3) xem agents live qua SSE. Đề xuất keys: `how.k`, `how.title`, `how.step1.title/desc` ('Cài collector' — 'Một file .mjs, không dependency, push transcript qua machine-token'), `how.step2.title/desc` ('Đăng nhập' — 'Tài khoản đầu tiên là owner, RBAC 4 vai trò'), `how.step3.title/desc` ('Xem live' — 'Trạng thái, thời gian chạy, việc đang làm — stream SSE'). Gắn `id="how"` vào section mới, trả `id` khác cho FeatureGrid.
  [cnt-2|P1|conf=high] Telemetry trong HUD là số tĩnh giả ('42' phiên live, '4m 12s', '1 stuck') trình bày như số liệu thật — rủi ro misleading
     EV: features.ts:58 `T('feat.1.t1', '42'), T('feat.1.t2', '4m 12s'), T('feat.1.t3', '1')` với label 'Phiên trực tiếp / Thời gian TB / Nghi kẹt' (landing.ts:45-47); hiển thị như stat thật trong panel 'Real-time monitoring' (desktop-1440-mech-30.png). Comment features.ts:28 thừa nhận đây là 'Universal value' cứng.
     REC: Không nên fetch số thật của org lên trang PUBLIC (lộ thông tin nội bộ khi chưa đăng nhập). Phương án an toàn: (a) gắn nhãn rõ là demo — thêm key `feat.demoBadge` { vi: 'DỮ LIỆU MÔ PHỎNG', en: 'SIMULATED', zh: '模拟数据' } hiển thị nhỏ trong HudPanel; hoặc (b) chỉ khi `isAuthed` (đã có sẵn ở page.tsx:9) mới thay bằng số thật từ /api/stats, còn khách lạ thấy badge demo. Các giá trị thuần fact ('AES-256', '$0', 'cron', 'SSE') giữ nguyên — chúng không misleading.
  [cnt-4|P1|conf=high] Nhiều tính năng thật của platform bị thiếu hẳn trên landing: Search full-text, map/geo tools, OCR 3 ngữ, cancellable workflow runs, trang /machines
     EV: Đối chiếu README.md:106 (Search `/search` full-text across sessions + conversations + workflows — đã port xong theo commit 3ad5b77), CLAUDE.md routes (map helpers `/api/geocode|reverse|route|nearby`, react-leaflet), CLAUDE.md OCR 'tesseract (vie+eng+chi_sim)', README.md:108 (workflow runs cancellable với nút Huỷ + run waterfall): KHÔNG có key nào trong landing.ts nhắc tới search, map/geo, ngôn ngữ OCR, hay cancellable runs. grid.world (landing.ts:137-142) chỉ nêu 'tìm web, tính toán, tự soi phiên' — bỏ qua geo tools.
     REC: Thêm 2 card vào GRID_FEATURES: `grid.search.title/desc` { vi: 'Tìm kiếm toàn văn', desc: 'Tìm xuyên phiên agent, hội thoại và workflow — một ô tìm kiếm cho cả tổ chức.' } và `grid.map.title/desc` { vi: 'Bản đồ & định vị', desc: 'Geocode, chỉ đường, tìm địa điểm gần — trợ lý trả lời kèm bản đồ tương tác.' }. Mở rộng feat.2.desc nêu OCR 3 ngữ (vie/eng/zh) — khớp luôn với pitch '3 ngôn ngữ'. Thêm 1 câu vào feat.4.desc: 'chạy theo lịch cron, theo dõi run waterfall và huỷ giữa chừng được.'
REFUTED:
P2:
  [cnt-3] Mâu thuẫn ngay trong 1 panel: status '7 LINKED' + telemetry '7' nhưng mô tả chỉ nêu 6 dịch vụ — Đồng bộ con số với danh sách tên: đổi status/telemetry thành '6' (đếm dịch vụ thật), hoặc nếu giữ 7 thì sửa desc nêu đủ. Với audience dev nội bộ, con số khớp danh sách quan trọng hơn con số to.
  [cnt-5] Không có section Security / Privacy pitch riêng, không so sánh chi phí $0 vs cloud, không FAQ — Ưu tiên 1: gom thành section 'Riêng tư & an toàn' (keys `security.k/title/sub` + 4 bullet `security.b1..b4`): dữ liệu không rời máy / credentials AES-256 per-user / mọi write qua cổng xác nhận + audit log / RBAC 4 vai trò. Ưu tiên 2: 1 hàng so sánh chi phí đơn giản (keys `cost.title`, `cost.local`, `cost.cloud`): 'Model local trên GPU của bạn: $0/tháng — API cloud tương đương: $xx/dev/tháng'. FAQ có thể bỏ qua với internal tool (giảm scope), nhưng security section thì nên có.
  [cnt-6] 'Screenshot' trong các HUD panel là faux (icon + scanline), không có ảnh sản phẩm thật nào trên trang — Thay shot box bằng screenshot thật đã xử lý (dashboard KPIs, chat vision đang OCR ảnh, workflow canvas, graph orchestrator→sub-agent) — với audience dev, ảnh thật thuyết phục hơn mọi copy. Thêm key alt `feat.N.shotAlt` cho cả 3 ngữ. Nếu chưa kịp chụp, ít nhất render mini-mockup dữ liệu (bảng session giả lập có nhãn demo) thay vì icon.
  [cnt-7] Copy vi có chỗ dịch gượng và 1 title không nhất quán giữa 3 ngữ — Sửa hero vi thành cấu trúc tự nhiên, ví dụ: title 'Nhìn đội agent của bạn' + accent 'vận hành sống động.' hoặc 'Mọi agent trên máy bạn' + 'hiện ra trực tiếp.'; dịch grid.world.title vi thành 'Công cụ thế giới thực' (hoặc giữ English cho CẢ 3 ngữ nếu coi là tên riêng — chọn 1 hướng); thống nhất feat.2 ('Local AI assistant · $0' cho en hoặc 'Chat AI cục bộ · $0' cho vi).
  [cnt-8] Model name hardcode 'qwen3-vl' trên landing trong khi tài liệu dự án nói primary model là gemma4:e4b — nội dung dễ stale — Tránh hardcode tên model cụ thể trên marketing copy: dùng mô tả bền hơn ('VLM cục bộ', 'local VLM · 8B'), hoặc nếu muốn show tên model thì đọc từ cùng nguồn cấu hình (DEFAULT_CHAT_MODEL) thay vì chuỗi tĩnh — tránh việc đổi model xong landing nói sai.
  [cnt-9] Metadata SEO/share của trang public là mô tả dev-facing, không bán platform — Export `metadata` riêng cho src/app/page.tsx với description marketing khớp hero.sub: 'Giám sát real-time các Claude agent đa máy, trợ lý AI cục bộ $0, connectors và workflow — tất cả chạy local.' (kèm bản en cho og:description nếu cần). Cân nhắc bỏ 'v2' khỏi title public.
  [cnt-10] Stack row ở footer liệt kê 'Three.js' — thư viện hiệu ứng của chính landing, không phải năng lực platform — Thay 'Three.js' bằng token nói về platform: 'SSE' hoặc 'Docker' hoặc 'Tailscale' — người xem click 'Công nghệ' muốn biết platform chạy bằng gì, không phải landing page render bằng gì.

========== PERF — web performance engineer — SCORE 7/10 ==========
SUMMARY: Landing page có nền tảng performance tốt hơn mặt bằng chung của trang "hiệu ứng nặng": LCP là text SSR với system font (không webfont, không ảnh), three.js được tách chunk đúng cách (dynamic ssr:false, chỉ tải khi cuộn tới), scroll progress dùng rAF + ref + state lượng tử hoá nên không re-render mỗi frame. Tuy nhiên có 3 vấn đề P1 ở phần "chạy liên tục": (1) WebGL context bị tạo/huỷ lặp lại do mount/unmount Mech3D theo IntersectionObserver — khớp với lỗi "THREE.WebGLRenderer: Context Lost" đã ghi nhận ở console khi resize; (2) animation scanline/cue animate thuộc tính `top` (layout) vô hạn trên 6 panel kể cả khi panel đang ẩn bằng opacity; (3) canvas dot-field cấp phát 1–2 radial gradient cho mỗi particle mỗi frame (tới ~120 particle, full viewport, composite 'lighter') và chạy suốt vòng đời trang. Trên máy dev RTX 5070 Ti mọi thứ mượt, nhưng trên laptop văn phòng/pin các chi phí này cộng dồn đúng vào đoạn showcase — nơi WebGL + canvas 2D + layout animation chạy đồng thời. Không có P0: trang hoạt động đầy đủ trong mọi screenshot.
STRENGTHS:
  + LCP gần như tối ưu: hero là text SSR (h1 gradient-clip), font hệ thống Segoe UI, không có ảnh/webfont — LCP chỉ phụ thuộc TTFB + CSS (src/components/landing/landing.module.css:23, Hero.tsx; desktop-1440-hero.png xác nhận hero thuần text).
  + Three.js tách chunk đúng: dynamic(() => import('./Mech3D'), { ssr: false }) tại MechShowcase.tsx:14, chỉ render khi IntersectionObserver báo gần viewport (rootMargin 200px, dòng 66) — three/fiber/drei (~200KB+ gzip) không lọt vào bundle hero, không ảnh hưởng LCP.
  + useScrollProgress.ts thiết kế chuẩn: listener passive + throttle bằng rAF (ticking flag, dòng 29-36), progressRef cập nhật mỗi frame cho mech 3D không qua React, còn state `progress` lượng tử hoá 0.02 nên cả đoạn cuộn 380vh chỉ re-render ~50 lần thay vì mỗi frame.
  + Dot-field có nhiều guard tốt: DPR cap 2 (useDotField.ts:64), số particle scale theo diện tích viewport min(120, w*h/13000) — chỉ ~25 particle trên mobile 390×844 (dòng 59), prefers-reduced-motion vẽ tĩnh 1 lần (dòng 115), resize debounce 150ms (dòng 110).
  + Mech 3D không tải asset ngoài: geometry procedural (RoundedBox primitives, MechModel.tsx), env map bake cục bộ bằng RoomEnvironment + PMREM (Mech3D.tsx:11-24) — không fetch GLTF/HDRI từ CDN, đúng tinh thần local-first; dpr={[1,2]} cap đúng.
  + LandingNav scroll handler passive và set state boolean — React bail-out khi giá trị không đổi nên không có re-render storm (LandingNav.tsx:20-25).
  + Fallback không-WebGL/reduced-motion render 6 panel tĩnh dạng grid (MechShowcase.tsx:81-94) — không bắt máy yếu trả chi phí 3D; CSS reduced-motion tắt toàn bộ animation vô hạn (landing.module.css:193-197).
  + lucide-react dùng named import (features.ts:6-20) — tree-shake tốt, chỉ ship ~12 icon.
CONFIRMED:
  [perf-2|P1|conf=high] Animation scanline/cue animate thuộc tính `top` (layout) vô hạn — chạy cả trên 6 panel đang ẩn
     EV: landing.module.css:145-146 — `.scanline { top: -50%; animation: scan 3.4s linear infinite } @keyframes scan { to { top: 100% } }`: animate `top` là thuộc tính layout → invalidate layout + paint mỗi frame trên main thread, không offload được sang compositor. Có 6 instance (1 trong mỗi HudPanel, HudPanel.tsx:38) và đều render đồng thời trong section sticky; `.coHidden { opacity: 0 }` (dòng 167) KHÔNG dừng animation — panel ẩn vẫn trả chi phí layout mỗi frame. Tương tự `.scrollBar::after` ở hero: `@keyframes cue` animate `top` (dòng 108-109). Đoạn showcase là nơi main thread đã gánh đồng thời WebGL render loop + rAF dot-field.
     REC: Đổi cả hai keyframes sang `transform: translateY(...)` (compositor-only, gần như miễn phí). Thêm `visibility: hidden` vào .coHidden (visibility dừng cả hit-testing và giúp browser skip paint) hoặc set `animation-play-state: paused` cho panel chưa reveal. Tác động ước lượng: TRUNG BÌNH (6 layout invalidation/frame suốt thời gian ở section mech; trên CPU yếu cộng dồn với issue perf-3 gây rớt frame đúng lúc hiệu ứng đinh của trang đang chạy). Sửa rất rẻ — 2 dòng CSS.
  [perf-3|P1|conf=high] Dot-field cấp phát 1-2 radial gradient mỗi particle mỗi frame, full viewport, chạy suốt vòng đời trang
     EV: useDotField.ts:79-90 — trong draw() mỗi particle loại 's' tạo 2 createRadialGradient, loại 'm'/'b' tạo 1, mỗi frame: với ~120 particle ở desktop × 60fps ≈ 10.000-14.000 object gradient cấp phát/giây (gradient không cache được, là anti-pattern canvas kinh điển) + GC pressure. Mỗi frame clearRect full canvas tới 2880×1620px ở DPR 2 (dòng 64-66, 104) với composite 'lighter' (dòng 105) và bokeh bán kính tới 86×DPR=172px → overdraw lớn. Vòng lặp rAF tự re-schedule vô điều kiện (dòng 107) — chạy từ lúc load đến lúc rời trang, kể cả khi user đứng yên đọc text hoặc khi WebGL mech đang render đè lên. (rAF tự pause khi tab ẩn nên không cần visibilitychange; reduced-motion đã xử lý đúng ở dòng 115.)
     REC: Pre-render mỗi particle thành sprite offscreen canvas MỘT lần lúc build() (vẽ gradient vào canvas nhỏ theo bán kính), rồi mỗi frame chỉ drawImage + globalAlpha — giảm ~90% chi phí raster và loại bỏ hoàn toàn cấp phát gradient per-frame. Cân nhắc giảm fps mục tiêu của layer nền xuống 30fps (skip frame xen kẽ) — mắt người không phân biệt được với hiệu ứng trôi chậm này. Tác động ước lượng: CAO trên laptop không GPU rời/pin (đây là chi phí nền liên tục của CẢ phiên xem trang), THẤP trên máy dev RTX — đúng loại lỗi không thấy được trên máy mạnh.
REFUTED:
  [perf-1] WebGL context bị tạo/huỷ lặp lại khi cuộn qua lại section mech — khớp lỗi 'Context Lost' đã ghi nhận — note: Các trích dẫn file:line đều đúng, nhưng kết luận P1 sai: @react-three/fiber gọi gl.forceContextLoss() + dispose khi Canvas unmount (events-b389eeca.esm.js:15860-15881) nên context không tích lũy đến giới hạn browser, và không tìm thấy bất kỳ ghi nhận 'Context Lost' nào trong repo/screenshots (Lighthouse errors-in-console pass, items rỗng). Phần còn lại (PMREM rebake, dead-code frameloop) là perf nit mức P3, không phải P1.
P2:
  [perf-4] Chunk three.js + khởi tạo WebGL + bake PMREM đều dồn vào đúng thời điểm user cuộn tới section — Prefetch chunk sớm tách khỏi mount: gọi `import('./Mech3D')` trong requestIdleCallback sau khi hero render xong (chunk nằm sẵn trong cache, không ảnh hưởng LCP vì network lúc đó đã rảnh), giữ IntersectionObserver chỉ để quyết định thời điểm mount/chạy. Hoặc tối thiểu tăng rootMargin lên ~1 viewport ('100%'). Tác động ước lượng: TRUNG BÌNH trên mạng chậm (hiệu ứng đinh xuất hiện trễ/khựng ở lần xem đầu), THẤP trên localhost.
  [perf-5] Mỗi bước progress re-render cả 6 HudPanel dù nội dung panel không đổi — Bọc HudPanel bằng React.memo — props đã stable sẵn nên chỉ cần 1 dòng, panel chỉ render lại khi đổi ngôn ngữ. Phần className toggle giữ ở wrapper div trong MechShowcase (đã đúng cấu trúc). Tác động ước lượng: THẤP-TRUNG BÌNH (React render là chi phí phụ so với perf-2/perf-3, nhưng cùng xảy ra trong lúc cuộn section nặng nhất; sửa gần như miễn phí).
  [perf-6] Toàn bộ landing là 'use client' — prose tĩnh và từ điển 3 ngôn ngữ đều ship + hydrate phía client — Đây là trade-off có chủ đích: nút đổi ngôn ngữ vi/en/zh đổi text tức thì qua context (LandingNav.tsx:48, provider.tsx:17-20) nên không thể server-hoá phần prose mà không mất tính năng đó. Giữ nguyên là chấp nhận được cho trang này; nếu muốn tối ưu, hướng đi là RSC hoá Hero/Footer/FeatureGrid và đổi ngôn ngữ bằng cookie + router.refresh() — chỉ đáng làm nếu đo thấy TBT thực tế cao. Tác động ước lượng: THẤP (vài chục KB + hydrate một cây nông).
  [perf-7] Cảnh báo 'THREE.Clock deprecated' — lệch version giữa three và @react-three/fiber — Nâng @react-three/fiber + drei lên bản đã chuyển sang Timer (kiểm tra changelog fiber 9.x mới nhất), hoặc pin three về dải version mà fiber 9.6 hỗ trợ chính thức. Tác động ước lượng: THẤP (hiện chỉ là noise console + log mỗi frame đầu, nhưng là tín hiệu API nội bộ sẽ vỡ ở lần nâng three tiếp theo).