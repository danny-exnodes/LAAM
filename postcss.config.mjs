// Tailwind CSS v4 uses a PostCSS plugin (no tailwind.config.js needed — theme
// lives in src/app/globals.css via @theme).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
