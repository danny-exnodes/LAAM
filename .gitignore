node_modules/
.DS_Store
*.log

# local env / secrets — never commit real tokens (keep the template)
.env
.env.*
!.env.example

# user-entered connector credentials (tokens) — NEVER commit
connectors.json
.laam/

# screenshot/test proof artifacts
chat-render-proof/
responsive-proof/
*-proof/
