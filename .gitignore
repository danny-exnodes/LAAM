# deps & build
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# env / secrets (never commit real values — keep .env.example)
.env
.env.local
.env*.local
.env.*
!.env.example

# SearXNG config holds a secret_key — keep the .example template, ignore the live file
searxng/settings.yml

# user-entered connector credentials / local data — NEVER commit
connectors.json
.laam/

# logs & os
*.log
.DS_Store

# screenshot / test proof artifacts
*-proof/

# agent worktrees (other sessions)
.claude/worktrees/

# superpowers brainstorming visual-companion mockups (local, throwaway)
.superpowers/

# DB backups (scripts/backup-db.ps1)
backups/

.claude/tmp/

# TTS engine audition samples (local A/B comparison WAVs, not project deliverables)
tts-samples/
