# deps & build
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# env / secrets (never commit real values — keep .env.example)
.env
.env.local
.env*.local
.env.*
!.env.example

# user-entered connector credentials / local data — NEVER commit
connectors.json
.laam/

# logs & os
*.log
.DS_Store

# screenshot / test proof artifacts
*-proof/

# agent worktrees (other sessions)
.claude/worktrees/
